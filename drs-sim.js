/* IDCOL DRS demo — shared state engine (seed · rules · scenario). Vanilla JS, localStorage-backed.
   Every portal page and the Simulation Console use the same functions. */
(function (global) {
  'use strict';
  var KEY = 'drs-sim-state-v5';
  var SEED_VERSION = 5;
  var S = null;
  var hasWindow = typeof window !== 'undefined';
  var store = hasWindow && window.localStorage ? window.localStorage : { _m: {}, getItem: function (k) { return this._m[k] || null; }, setItem: function (k, v) { this._m[k] = v; }, removeItem: function (k) { delete this._m[k]; } };

  /* ---------- utils ---------- */
  function pad(n, w) { w = w || 4; var s = String(n); while (s.length < w) s = '0' + s; return s; }
  function rng(seed) { var t = seed >>> 0; return function () { t += 0x6D2B79F5; var r = Math.imul(t ^ (t >>> 15), 1 | t); r ^= r + Math.imul(r ^ (r >>> 7), 61 | r); return ((r ^ (r >>> 14)) >>> 0) / 4294967296; }; }
  function parseD(s) { var p = s.split('-'); return new Date(Date.UTC(+p[0], +p[1] - 1, +p[2])); }
  function iso(d) { return d.toISOString().slice(0, 10); }
  function addDays(s, n) { var d = parseD(s); d.setUTCDate(d.getUTCDate() + n); return iso(d); }
  function addMonths(s, n) { var d = parseD(s); var day = d.getUTCDate(); d.setUTCDate(1); d.setUTCMonth(d.getUTCMonth() + n); var last = new Date(Date.UTC(d.getUTCFullYear(), d.getUTCMonth() + 1, 0)).getUTCDate(); d.setUTCDate(Math.min(day, last)); return iso(d); }
  function diffDays(a, b) { return Math.round((parseD(a) - parseD(b)) / 86400000); }
  var MONTHS = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
  function fmtDate(s) { if (!s) return '—'; var d = parseD(s.slice(0, 10)); return d.getUTCDate() + ' ' + MONTHS[d.getUTCMonth()] + ' ' + d.getUTCFullYear(); }
  function fmtDT(s) { if (!s) return '—'; return fmtDate(s) + (s.length > 10 ? ' ' + s.slice(11, 16) : ''); }
  function groupBD(n) { n = Math.round(n); var neg = n < 0; n = Math.abs(n); var s = String(n); if (s.length <= 3) return (neg ? '-' : '') + s; var last3 = s.slice(-3), rest = s.slice(0, -3), out = ''; while (rest.length > 2) { out = ',' + rest.slice(-2) + out; rest = rest.slice(0, -2); } return (neg ? '-' : '') + rest + out + ',' + last3; }
  function money(n, opts) { opts = opts || {}; if (n == null) return '—'; if (opts.short) { var a = Math.abs(n); if (a >= 1e7) return '৳' + (n / 1e7).toFixed(2) + ' Cr'; if (a >= 1e5) return '৳' + (n / 1e5).toFixed(2) + ' L'; } return '৳' + groupBD(n); }
  function num(n, d) { if (n == null) return '—'; d = d == null ? 0 : d; return Number(n).toLocaleString('en-US', { minimumFractionDigits: d, maximumFractionDigits: d }); }
  function pct(n, d) { return n == null ? '—' : Number(n).toFixed(d == null ? 1 : d) + '%'; }
  function uid(prefix) { S.seq = (S.seq || 1000) + 1; return prefix + '-' + S.seq; }
  function nowTs() { return S.sim.today + 'T' + S.sim.clock; }
  function tick() { var p = S.sim.clock.split(':'); var m = (+p[0]) * 60 + (+p[1]) + 3 + Math.floor(Math.random() * 4); if (m > 23 * 60 + 50) m = 8 * 60; S.sim.clock = pad(Math.floor(m / 60), 2) + ':' + pad(m % 60, 2); return nowTs(); }
  function clone(o) { return JSON.parse(JSON.stringify(o)); }
  function byId(arr, id, key) { key = key || 'id'; for (var i = 0; i < arr.length; i++) if (arr[i][key] === id) return arr[i]; return null; }

  /* ---------- seed ---------- */
  var DISTRICT_CODE = { Dhaka: 'DHA', Gazipur: 'GAZ', Khulna: 'KHU', Jashore: 'JSR', Satkhira: 'SAT', Chattogram: 'CTG', Cumilla: 'CUM', Rajshahi: 'RAJ', Sylhet: 'SYL', Barishal: 'BAR' };
  var PACKAGES = [
    { kwp: 3, benchmark: 270000, ceiling: 290000, maxRefinance: 190000, panels: 6 },
    { kwp: 4, benchmark: 340000, ceiling: 360000, maxRefinance: 240000, panels: 8 },
    { kwp: 5, benchmark: 410000, ceiling: 440000, maxRefinance: 290000, panels: 9 }
  ];
  var CATALOGUE = [
    { id: 'CAT-P01', category: 'panel', brand: 'Jinko', model: 'Tiger Neo 550 W', keySpec: '550 Wp mono N-type', certRef: 'IEC 61215 / 61730', tscRef: 'TSC-2026-004', tscDate: '2026-04-12', status: 'active', prefix: 'JKM550' },
    { id: 'CAT-P02', category: 'panel', brand: 'Longi', model: 'Hi-MO 6 545 W', keySpec: '545 Wp mono HPBC', certRef: 'IEC 61215 / 61730', tscRef: 'TSC-2026-005', tscDate: '2026-04-12', status: 'active', prefix: 'LNG545' },
    { id: 'CAT-I01', category: 'inverter', brand: 'Growatt', model: 'MIN 5000TL-X', keySpec: '5 kW · 2 MPPT · hybrid-ready', certRef: 'IEC 62109 · BDS', tscRef: 'TSC-2026-006', tscDate: '2026-04-19', status: 'active', prefix: 'GRW5K', api: { read: 'Yes', write: 'Yes (partner API)', terms: 'OEM partner agreement signed', fallback: 'Modbus RTU' } },
    { id: 'CAT-I02', category: 'inverter', brand: 'Solis', model: 'S6-GR1P5K', keySpec: '5 kW · 2 MPPT', certRef: 'IEC 62109', tscRef: 'TSC-2026-006', tscDate: '2026-04-19', status: 'active', prefix: 'SOL5K', api: { read: 'Yes', write: 'Yes (SolisCloud control)', terms: 'Commercial API key', fallback: 'Modbus RTU' } },
    { id: 'CAT-I03', category: 'inverter', brand: 'Sungrow', model: 'SG5.0RS', keySpec: '5 kW · 2 MPPT', certRef: 'IEC 62109', tscRef: 'TSC-2026-007', tscDate: '2026-05-03', status: 'active', prefix: 'SGR5K', api: { read: 'Yes', write: 'Partial (schedule only)', terms: 'iSolarCloud developer terms', fallback: 'SunSpec' } },
    { id: 'CAT-I04', category: 'inverter', brand: 'Huawei', model: 'SUN2000-5KTL-L1', keySpec: '5 kW · 2 MPPT', certRef: 'IEC 62109', tscRef: 'TSC-2026-007', tscDate: '2026-05-03', status: 'active', prefix: 'HWI5K', api: { read: 'Yes', write: 'Yes (FusionSolar NBI)', terms: 'Northbound interface licence', fallback: 'Modbus TCP' } },
    { id: 'CAT-B01', category: 'battery', brand: 'BYD', model: 'Battery-Box HVS 5.1', keySpec: '5.12 kWh LFP', certRef: 'IEC 62619 · UN38.3', tscRef: 'TSC-2026-008', tscDate: '2026-05-03', status: 'active', prefix: 'BYD51' },
    { id: 'CAT-B02', category: 'battery', brand: 'Pylontech', model: 'US5000', keySpec: '4.8 kWh LFP', certRef: 'IEC 62619', tscRef: 'TSC-2026-008', tscDate: '2026-05-03', status: 'active', prefix: 'PYL48' },
    { id: 'CAT-I05', category: 'inverter', brand: 'SMA', model: 'Sunny Boy 5.0', keySpec: '5 kW · 3 MPPT', certRef: 'IEC 62109', tscRef: 'TSC-2026-011', tscDate: '2026-07-14', status: 'suspended', prefix: 'SMA5K', api: { read: 'Yes', write: 'No', terms: 'Read-only Sunny Portal API', fallback: 'SunSpec' } }
  ];
  var ENLIST = [
    { id: 'ENL-0007', supplier: 'GoodWe Bangladesh Ltd', category: 'inverter', brand: 'GoodWe', model: 'GW5000-DNS-30', keySpec: '5 kW · 2 MPPT', docs: ['Datasheet', 'IEC 62109 certificate', 'Type test report', 'SEMS API capability letter'], submittedAt: '2026-09-02', status: 'Pending', api: { read: 'Yes', write: 'Partner terms pending', fallback: 'Modbus' } },
    { id: 'ENL-0008', supplier: 'Trina Solar (Dhaka agent)', category: 'panel', brand: 'Trina', model: 'Vertex S+ 445 W', keySpec: '445 Wp', docs: ['Datasheet', 'IEC 61215', 'IEC 61730'], submittedAt: '2026-09-06', status: 'Pending' },
    { id: 'ENL-0004', supplier: 'ABB Power (local distributor)', category: 'inverter', brand: 'ABB', model: 'UNO-DM-5.0', keySpec: '5 kW', docs: ['Datasheet'], submittedAt: '2026-06-20', status: 'Rejected', tscRef: 'TSC-2026-010', tscDate: '2026-07-14', reason: 'Product line discontinued; no local service partner' },
    { id: 'ENL-0003', supplier: 'Jinko Solar (BD)', category: 'panel', brand: 'Jinko', model: 'Tiger Neo 550 W', keySpec: '550 Wp', docs: ['Datasheet', 'IEC 61215', 'IEC 61730', 'PID test'], submittedAt: '2026-03-28', status: 'Approved', tscRef: 'TSC-2026-004', tscDate: '2026-04-12' },
    { id: 'ENL-0005', supplier: 'Growatt New Energy', category: 'inverter', brand: 'Growatt', model: 'MIN 5000TL-X', keySpec: '5 kW', docs: ['Datasheet', 'IEC 62109', 'API capability letter'], submittedAt: '2026-04-01', status: 'Approved', tscRef: 'TSC-2026-006', tscDate: '2026-04-19' }
  ];
  var CHECKLIST = ['Mounting structure per standard (wind load, tilt)', 'Panel count and wattage match package', 'DC cabling, MC4 connectors, conduit', 'Earthing and lightning protection', 'Inverter mounting, ventilation, labelling', 'AC isolation and protection (MCB/RCD)', 'Battery installation, ventilation, BMS', 'Net-meter application / meter installed', 'Inverter commissioning & Wi-Fi/4G dongle online', 'Household briefed on operation & safety', 'Site GPS within 100 m of onboarding GPS', 'Photos: roof, inverter, battery, meter, serials'];

  function makeDA(o) {
    return {
      id: o.id, name: o.name, legalForm: o.legalForm, registrationNo: o.registrationNo, districts: o.districts, status: 'Active', onboardedAt: o.onboardedAt,
      keyPersons: o.keyPersons, highlights: o.highlights, solarTrackRecord: o.solarTrackRecord,
      assessment: o.assessment || { accounting: 'Satisfactory', mis: 'Satisfactory', internalAudit: 'Adequate' }, essf: o.essf || 'ESSF compliance plan submitted',
      facility: { limit: o.limit, rate: 5, tenorMonths: 84, graceMonths: 6, frequency: 'Monthly', availability: '2026-06-01 → 2027-12-31', tranches: o.tranches || 2 },
      advance: { limit: o.advanceLimit, rate: 4, tenorMonths: 24, outstanding: o.advanceOutstanding || 0, security: 'Bank Guarantee (advance)' },
      security: { bgAmount: o.bg, bgBank: o.bgBank, bgExpiry: o.bgExpiry, dsraBalance: o.dsra, personalGuarantees: o.pg || [], corporateGuarantees: o.cg || [], liens: ['Proceeds Account', 'DSRA'], hypothecation: o.hyp || 'Registered with RJSC', dpNote: true, letterOfContinuity: true, undatedCheques: 3 },
      disbursementBlock: false
    };
  }
  var DAS = [
    makeDA({ id: 'DA-001', name: 'Grameen Shakti Ltd', legalForm: 'MFI', registrationNo: 'MRA-00012', districts: ['Dhaka', 'Gazipur'], onboardedAt: '2026-05-15', keyPersons: ['Md. Rashed Karim — Managing Director', 'Farzana Islam — CFO'], highlights: [{ year: 2023, revenue: 412, assets: 1580, netIncome: 38 }, { year: 2024, revenue: 465, assets: 1720, netIncome: 44 }, { year: 2025, revenue: 528, assets: 1910, netIncome: 51 }], solarTrackRecord: '1.9 million SHS installed 2003–2021; 14 rooftop pilots 2025', limit: 500000000, advanceLimit: 50000000, advanceOutstanding: 12000000, bg: 60000000, bgBank: 'Sonali Bank', bgExpiry: '2027-06-30', dsra: 4200000, pg: ['Md. Rashed Karim (MD)'], cg: ['Grameen Trust'] }),
    makeDA({ id: 'DA-002', name: 'BRAC Solar Programme', legalForm: 'NGO', registrationNo: 'NGOAB-0231', districts: ['Khulna', 'Jashore'], onboardedAt: '2026-05-22', keyPersons: ['Tahmina Rahman — Programme Head', 'S. M. Alam — Finance Lead'], highlights: [{ year: 2023, revenue: 310, assets: 980, netIncome: 22 }, { year: 2024, revenue: 344, assets: 1050, netIncome: 25 }, { year: 2025, revenue: 380, assets: 1140, netIncome: 29 }], solarTrackRecord: '620k SHS; 40 rooftop systems 2024–25', limit: 400000000, advanceLimit: 40000000, advanceOutstanding: 8000000, bg: 45000000, bgBank: 'BRAC Bank', bgExpiry: '2027-03-31', dsra: 3100000, pg: ['Tahmina Rahman (ED)'], cg: [] }),
    makeDA({ id: 'DA-003', name: 'SolarNet Bangladesh Ltd', legalForm: 'Private', registrationNo: 'RJSC-C-188234', districts: ['Chattogram', 'Cumilla'], onboardedAt: '2026-06-03', keyPersons: ['Anisur Rahman — CEO', 'Nusrat Jahan — Director Finance'], highlights: [{ year: 2023, revenue: 96, assets: 210, netIncome: 6 }, { year: 2024, revenue: 128, assets: 260, netIncome: 9 }, { year: 2025, revenue: 171, assets: 330, netIncome: 12 }], solarTrackRecord: '2.4 MWp C&I rooftop EPC 2022–25', limit: 250000000, advanceLimit: 25000000, advanceOutstanding: 6000000, bg: 18000000, bgBank: 'Eastern Bank', bgExpiry: '2026-11-15', dsra: 900000, pg: ['Anisur Rahman (CEO)', 'Nusrat Jahan (Director)'], cg: ['SolarNet Holdings'] })
  ];
  var HH = [
    // DA-001 Grameen Shakti
    { systemId: 'DRS-DHA-0001', daId: 'DA-001', name: 'Abdul Halim', nid: '1985123456789', phone: '01711-234501', district: 'Dhaka', upazila: 'Savar', address: 'House 12, Road 3, Hemayetpur', gps: { lat: 23.7925, lng: 90.2611 }, kwp: 4, price: 340000, installed: '2026-06-14', brand: 'CAT-I01', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'normal' },
    { systemId: 'DRS-DHA-0002', daId: 'DA-001', name: 'Shirin Akter', nid: '1990123456790', phone: '01711-234502', district: 'Dhaka', upazila: 'Keraniganj', address: 'Kalindi, Zinzira', gps: { lat: 23.6931, lng: 90.3742 }, kwp: 3, price: 270000, installed: '2026-06-20', brand: 'CAT-I01', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'reversal' },
    { systemId: 'DRS-DHA-0003', daId: 'DA-001', name: 'Kamal Uddin', nid: '1978123456791', phone: '01711-234503', district: 'Dhaka', upazila: 'Dhamrai', address: 'Sutipara Bazar', gps: { lat: 23.9204, lng: 90.2158 }, kwp: 5, price: 410000, installed: '2026-06-28', brand: 'CAT-I01', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'normal' },
    { systemId: 'DRS-DHA-0004', daId: 'DA-001', name: 'Rafiqul Islam', nid: '1982123456792', phone: '01711-234504', district: 'Dhaka', upazila: 'Savar', address: 'Ashulia, Jamgora', gps: { lat: 23.9310, lng: 90.2741 }, kwp: 4, price: 340000, installed: '2026-05-28', brand: 'CAT-I01', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'halted' },
    { systemId: 'DRS-GAZ-0031', daId: 'DA-001', name: 'Monowara Begum', nid: '1975123456793', phone: '01711-234505', district: 'Gazipur', upazila: 'Sreepur', address: 'Mawna Chowrasta', gps: { lat: 24.1102, lng: 90.4145 }, kwp: 4, price: 345000, installed: '2026-07-05', brand: 'CAT-I01', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'manualhistory' },
    { systemId: 'DRS-GAZ-0032', daId: 'DA-001', name: 'Jahangir Alam', nid: '1988123456794', phone: '01711-234506', district: 'Gazipur', upazila: 'Kaliakair', address: 'Chandra, Safipur', gps: { lat: 24.0716, lng: 90.2213 }, kwp: 5, price: 420000, installed: '2026-07-12', brand: 'CAT-I01', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'fault' },
    // DA-002 BRAC
    { systemId: 'DRS-KHU-0201', daId: 'DA-002', name: 'Nasima Khatun', nid: '1986123456795', phone: '01811-334501', district: 'Khulna', upazila: 'Batiaghata', address: 'Jalma Union', gps: { lat: 22.7301, lng: 89.5210 }, kwp: 3, price: 275000, installed: '2026-06-25', brand: 'CAT-I02', panel: 'CAT-P02', battery: 'CAT-B02', profile: 'normal' },
    { systemId: 'DRS-KHU-0202', daId: 'DA-002', name: 'Salam Sheikh', nid: '1979123456796', phone: '01811-334502', district: 'Khulna', upazila: 'Dumuria', address: 'Chuknagar', gps: { lat: 22.8135, lng: 89.4157 }, kwp: 4, price: 340000, installed: '2026-07-02', brand: 'CAT-I02', panel: 'CAT-P02', battery: 'CAT-B02', profile: 'underperf' },
    { systemId: 'DRS-JSR-0055', daId: 'DA-002', name: 'Rina Das', nid: '1991123456797', phone: '01811-334503', district: 'Jashore', upazila: 'Jhikargachha', address: 'Gadkhali', gps: { lat: 23.0966, lng: 89.0217 }, kwp: 4, price: 350000, installed: '2026-07-18', brand: 'CAT-I02', panel: 'CAT-P02', battery: 'CAT-B02', profile: 'normal' },
    { systemId: 'DRS-JSR-0056', daId: 'DA-002', name: 'Habibur Rahman', nid: '1983123456798', phone: '01811-334504', district: 'Jashore', upazila: 'Sadar', address: 'Chanchra', gps: { lat: 23.1665, lng: 89.2081 }, kwp: 5, price: 425000, installed: '2026-08-21', brand: 'CAT-I02', panel: 'CAT-P02', battery: 'CAT-B02', profile: 'mismatch' },
    // DA-003 SolarNet
    { systemId: 'DRS-CTG-0101', daId: 'DA-003', name: 'Fatema Begum', nid: '1987123456799', phone: '01911-434501', district: 'Chattogram', upazila: 'Hathazari', address: 'Fatehpur', gps: { lat: 22.5012, lng: 91.8098 }, kwp: 4, price: 340000, installed: '2026-07-08', brand: 'CAT-I03', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'notexport' },
    { systemId: 'DRS-CTG-0102', daId: 'DA-003', name: 'Mizanur Rahman', nid: '1980123456800', phone: '01911-434502', district: 'Chattogram', upazila: 'Sitakunda', address: 'Bhatiary', gps: { lat: 22.4331, lng: 91.7455 }, kwp: 3, price: 280000, installed: '2026-07-15', brand: 'CAT-I03', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'offline' },
    { systemId: 'DRS-CUM-0088', daId: 'DA-003', name: 'Sultana Razia', nid: '1992123456801', phone: '01911-434503', district: 'Cumilla', upazila: 'Daudkandi', address: 'Gauripur', gps: { lat: 23.5341, lng: 90.7207 }, kwp: 4, price: 340000, installed: '2026-08-02', brand: 'CAT-I03', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'normal' },
    { systemId: 'DRS-CUM-0089', daId: 'DA-003', name: 'Delwar Hossain', nid: '1984123456802', phone: '01911-434504', district: 'Cumilla', upazila: 'Sadar', address: 'Kandirpar', gps: { lat: 23.4607, lng: 91.1809 }, kwp: 5, price: 415000, installed: '2026-09-08', brand: 'CAT-I03', panel: 'CAT-P01', battery: 'CAT-B01', profile: 'pending' }
  ];

  function pkg(kwp) { return byId(PACKAGES, kwp, 'kwp'); }
  function emi(P, ratePct, n) { var r = ratePct / 100 / 12; if (r === 0) return Math.round(P / n / 10) * 10; var e = P * r * Math.pow(1 + r, n) / (Math.pow(1 + r, n) - 1); return Math.round(e / 10) * 10; }
  function buildSchedule(P, ratePct, n, commencement) { var inst = emi(P, ratePct, n), r = ratePct / 100 / 12, bal = P, rows = []; for (var i = 1; i <= n; i++) { var interest = Math.round(bal * r), principal = i === n ? bal : inst - interest; bal -= principal; rows.push({ n: i, dueDate: addMonths(commencement, i - 1), amount: i === n ? principal + interest : inst, principal: principal, interest: interest, paid: 0, paidAt: null }); } return { installment: inst, rows: rows }; }

  function seed() {
    var today = '2026-09-11';
    var st = { version: SEED_VERSION, seq: 1000, settings: { rate: 6, tenorMonths: 60, daRate: 5, daTenorMonths: 84, haltOverdueDays: 10, pendingHaltWarningDay: 8, connectivityHaltDays: 10, securityCoverPct: 20, bgNoticeDays: 90, allocation: 'Oldest overdue first, then current; excess held as credit', ews: { collectionEfficiency: 90, par30: 5, par90: 2, telemetryAvailability: 95, inspectionFailureRate: 10 }, packages: clone(PACKAGES) },
      das: clone(DAS), catalogue: clone(CATALOGUE), enlistment: clone(ENLIST), batches: [], inventory: [], households: [], inspections: [], claims: [], loans: [], payments: [], telemetry: {}, commands: [], notifications: [], audit: [], complaints: [], reversalRequests: [], exceptions: [], bankStatement: [], settingsPending: null, lastReconciliation: null, daLedgers: [], users: [
        { name: 'A. Karim', role: 'IDCOL · Program Unit (maker)', email: 'a.karim@idcol.org', perms: ['program', 'claims-review', 'halt', 'tickets'] }, { name: 'S. Chowdhury', role: 'IDCOL · Accounts & Finance (checker)', email: 's.chowdhury@idcol.org', perms: ['finance', 'claims-check', 'reversal-approve', 'settings', 'tickets'] }, { name: 'M. Hasan', role: 'IDCOL · Technical Unit', email: 'm.hasan@idcol.org', perms: ['technical', 'tickets'] }, { name: 'R. Sultana', role: 'IDCOL · Inspector', email: 'r.sultana@idcol.org', perms: ['inspection', 'tickets'] }, { name: 'T. Ahmed', role: 'IDCOL · Credit Admin (approver)', email: 't.ahmed@idcol.org', perms: ['finance', 'disburse', 'settings', 'block', 'halt', 'tickets'] }],
      sim: { today: today, clock: '10:05', step: 0, activeDaId: 'DA-001', activeSystemId: 'DRS-DHA-0001', lang: 'en', lastChanged: [], scenarioLog: [] } };
    S = st;
    var R = rng(20260911);
    function mkBatch(id, daId, catId, qty, supplier, status, verifiedAt, uploadedAt) {
      var cat = byId(st.catalogue, catId); var serials = []; for (var i = 1; i <= qty; i++) serials.push(cat.prefix + '-' + id.replace('B-', '') + '-' + pad(i));
      var b = { id: id, daId: daId, catalogueId: catId, quantity: qty, supplier: supplier, psiRef: 'PSI-' + id.replace('B-', '') + '-2026', psiUploadedAt: uploadedAt, status: status, verifiedBy: status === 'Verified' ? 'M. Hasan' : null, verifiedAt: status === 'Verified' ? verifiedAt : null, serials: serials, financedAmount: 0 };
      st.batches.push(b);
      if (status === 'Verified') serials.forEach(function (sn) { st.inventory.push({ serial: sn, batchId: id, daId: daId, catalogueId: catId, status: 'in_stock', systemId: null, updatedAt: verifiedAt }); });
      return b;
    }
    mkBatch('B-1001', 'DA-001', 'CAT-P01', 200, 'Jinko Solar (BD)', 'Verified', '2026-06-02', '2026-05-28');
    mkBatch('B-1002', 'DA-001', 'CAT-I01', 25, 'Growatt New Energy', 'Verified', '2026-06-02', '2026-05-28');
    mkBatch('B-1003', 'DA-001', 'CAT-B01', 25, 'BYD Battery (BD)', 'Verified', '2026-06-02', '2026-05-28');
    mkBatch('B-1011', 'DA-002', 'CAT-P02', 120, 'Longi Solar', 'Verified', '2026-06-12', '2026-06-08');
    mkBatch('B-1012', 'DA-002', 'CAT-I02', 15, 'Solis (Ginlong)', 'Verified', '2026-06-12', '2026-06-08');
    mkBatch('B-1013', 'DA-002', 'CAT-B02', 15, 'Pylontech', 'Verified', '2026-06-12', '2026-06-08');
    mkBatch('B-1021', 'DA-003', 'CAT-P01', 120, 'Jinko Solar (BD)', 'Verified', '2026-06-25', '2026-06-20');
    mkBatch('B-1022', 'DA-003', 'CAT-I03', 15, 'Sungrow', 'Verified', '2026-06-25', '2026-06-20');
    mkBatch('B-1023', 'DA-003', 'CAT-B01', 15, 'BYD Battery (BD)', 'Verified', '2026-06-25', '2026-06-20');
    mkBatch('B-1031', 'DA-003', 'CAT-I04', 10, 'Huawei Digital Power', 'Pending', null, '2026-09-09');

    function takeSerials(daId, catId, n) { var out = []; for (var i = 0; i < st.inventory.length && out.length < n; i++) { var u = st.inventory[i]; if (u.daId === daId && u.catalogueId === catId && u.status === 'in_stock') out.push(u); } return out; }
    var insSeq = 100, clmSeq = 100, paySeq = 200, cmdSeq = 500, ntfSeq = 1, audSeq = 1;
    function audit(ts, actor, role, action, entity, entityId, before, after, reason) { st.audit.push({ id: 'AUD-' + pad(audSeq++, 5), ts: ts, actor: actor, role: role, action: action, entity: entity, entityId: entityId, before: before || null, after: after || null, reason: reason || null }); }
    function notify(audience, type, message, at, bn) { st.notifications.push({ id: 'NTF-' + pad(ntfSeq++, 5), audience: audience, type: type, message: message, messageBn: bn || null, createdAt: at, read: false }); }

    HH.forEach(function (h) {
      var p = pkg(h.kwp);
      var panels = takeSerials(h.daId, h.panel, p.panels), inv = takeSerials(h.daId, h.brand, 1)[0], bat = takeSerials(h.daId, h.battery, 1)[0];
      panels.concat([inv, bat]).forEach(function (u) { u.status = 'installed'; u.systemId = h.systemId; u.updatedAt = h.installed; });
      var hh = { systemId: h.systemId, daId: h.daId, autopay: h.profile !== 'halted', name: h.name, nid: h.nid, phone: h.phone, district: h.district, upazila: h.upazila, address: h.address, gps: h.gps, packageKwp: h.kwp, contractPrice: h.price, benchmark: p.benchmark, ceiling: p.ceiling, panelSerials: panels.map(function (u) { return u.serial; }), inverterSerial: inv.serial, batterySerial: bat.serial, inverterCat: h.brand, panelCat: h.panel, batteryCat: h.battery, installationDate: h.installed, netMetering: h.profile === 'pending' ? 'Applied' : 'Approved', daReport: { checklistOk: true, photos: 6, installer: 'Field team ' + h.daId.slice(-1), uploadedAt: h.installed }, status: 'Active', inverterState: 'running', haltReason: null, haltedAt: null, offlineSince: null, pendingConnectivityHalt: false, createdAt: h.installed + 'T11:20' };
      st.households.push(hh);
      audit(h.installed + 'T11:20', 'DA agent', 'DA', 'HOUSEHOLD_ONBOARDED', 'household', h.systemId, null, { package: h.kwp + ' kWp', serials: p.panels + 2 });
      // loan
      var upfront = Math.round(h.price * 0.2), financed = h.price - upfront, refinanced = Math.min(p.maxRefinance, Math.round(financed * 0.88));
      var commencement = addMonths(h.installed, 1);
      var sch = buildSchedule(financed, st.settings.rate, st.settings.tenorMonths, commencement);
      var loan = { systemId: h.systemId, daId: h.daId, totalCost: h.price, upfront: upfront, daFinanced: financed, idcolRefinanced: refinanced, rate: st.settings.rate, tenorMonths: st.settings.tenorMonths, installment: sch.installment, commencement: commencement, maturity: addMonths(commencement, st.settings.tenorMonths - 1), schedule: sch.rows, credit: 0, notified: {} };
      st.loans.push(loan);
      // inspection + claim
      var insp = { id: 'INS-' + pad(insSeq++, 4), systemId: h.systemId, daId: h.daId, inspector: 'R. Sultana', scheduledAt: addDays(h.installed, 3), completedAt: null, checklist: CHECKLIST.map(function (c) { return { item: c, result: 'Pending' }; }), scanned: { panels: [], inverter: null, battery: null }, mismatch: null, gpsDistanceM: null, photos: 0, result: 'Pending', reason: null, log: [] };
      var claim = { id: 'CLM-' + pad(clmSeq++, 4), daId: h.daId, systemId: h.systemId, amount: refinanced, packageMax: p.maxRefinance, status: 'Not eligible', maker: null, checker: null, preparedAt: null, approvedAt: null, paidAt: null, holdReason: null, duplicateCheck: 'Clear' };
      var doneAt = addDays(h.installed, 4);
      if (h.profile === 'pending') { /* stays pending */ }
      else if (h.profile === 'mismatch') {
        insp.completedAt = doneAt + 'T14:10'; insp.checklist.forEach(function (c) { c.result = 'Pass'; }); insp.scanned = { panels: panels.map(function (u) { return u.serial; }), inverter: inv.serial.replace(/\d{4}$/, '0099'), battery: bat.serial }; insp.mismatch = true; insp.gpsDistanceM = 22; insp.photos = 6; insp.result = 'Exception'; insp.reason = 'Inverter serial on site does not match onboarding record (' + inv.serial + ')';
        insp.log.push({ ts: insp.completedAt, user: 'R. Sultana', action: 'Result → Exception', reason: insp.reason });
        claim.status = 'On hold'; claim.holdReason = insp.reason; audit(insp.completedAt, 'R. Sultana', 'IDCOL', 'INSPECTION_EXCEPTION', 'inspection', insp.id, 'Pending', 'Exception', insp.reason);
        notify('da:' + h.daId, 'inspection', 'Inspection ' + insp.id + ' for ' + h.systemId + ' raised an exception — claim on hold.', insp.completedAt);
      } else {
        insp.completedAt = doneAt + 'T11:30'; insp.checklist.forEach(function (c) { c.result = 'Pass'; }); insp.scanned = { panels: panels.map(function (u) { return u.serial; }), inverter: inv.serial, battery: bat.serial }; insp.mismatch = false; insp.gpsDistanceM = Math.round(5 + R() * 40); insp.photos = 6; insp.result = 'Pass';
        insp.log.push({ ts: insp.completedAt, user: 'R. Sultana', action: 'Result → Pass', reason: 'All serials match; checklist complete' });
        claim.status = 'Paid'; claim.maker = 'A. Karim'; claim.checker = 'S. Chowdhury'; claim.preparedAt = addDays(doneAt, 2) + 'T10:00'; claim.approvedAt = addDays(doneAt, 3) + 'T15:20'; claim.paidAt = addDays(doneAt, 5);
        audit(insp.completedAt, 'R. Sultana', 'IDCOL', 'INSPECTION_PASS', 'inspection', insp.id, 'Pending', 'Pass'); audit(claim.approvedAt, 'S. Chowdhury', 'IDCOL', 'CLAIM_APPROVED', 'claim', claim.id, 'Prepared', 'Approved');
      }
      st.inspections.push(insp); st.claims.push(claim);
      // payments (history)
      var channels = ['bKash', 'Nagad', 'Cash', 'Bank', 'Card'];
      var unpaidFrom = 999; // index from which installments stay unpaid
      if (h.profile === 'halted') unpaidFrom = 2; // installment 3 due 10 Aug… we want overdue 14 days → due date = today-14
      loan.schedule.forEach(function (row, idx) {
        if (row.dueDate > today) return;
        if (h.profile === 'halted' && idx >= 2) return; // leave unpaid
        var lag = Math.floor(R() * 5), payDate = addDays(row.dueDate, lag); if (payDate > today) payDate = today;
        var ch = channels[Math.floor(R() * channels.length)];
        var pmt = { id: 'PAY-' + pad(paySeq++, 5), receiptNo: 'RCPT-' + payDate.replace(/-/g, '').slice(2) + '-' + pad(paySeq, 4), systemId: h.systemId, daId: h.daId, date: payDate, amount: row.amount, channel: ch, reference: (ch === 'bKash' ? 'BK' : ch === 'Nagad' ? 'NG' : ch === 'Card' ? 'CD' : ch === 'Bank' ? 'NPSB' : 'CASH') + Math.floor(1e7 + R() * 9e7), status: 'Success', allocated: [{ n: row.n, amount: row.amount }], recordedBy: ch === 'Cash' || ch === 'Bank' ? 'DA collection agent' : 'Gateway (auto-post)', reconciled: true };
        row.paid = row.amount; row.paidAt = payDate; st.payments.push(pmt);
      });
      if (h.systemId === 'DRS-DHA-0001') { st.payments.push({ id: 'PAY-' + pad(paySeq++, 5), receiptNo: '—', systemId: h.systemId, daId: h.daId, date: '2026-08-14', ts: '2026-08-14T19:22', amount: loan.installment, channel: 'bKash', reference: 'BK44120077', status: 'Failed', stage: 'failed', allocated: [], recordedBy: 'Gateway (auto-post)', reconciled: true, failureReason: 'PSP timeout — no debit occurred; household retried successfully' }); }
      if (h.systemId === 'DRS-DHA-0003') { var rf = { id: 'PAY-' + pad(paySeq++, 5), receiptNo: 'RCPT-260805-' + pad(paySeq, 4), systemId: h.systemId, daId: h.daId, date: '2026-08-05', ts: '2026-08-05T10:14', amount: 2000, channel: 'Nagad', reference: 'NG77310025', status: 'Refunded', stage: 'refunded', allocated: [], recordedBy: 'Gateway (auto-post)', reconciled: true, refundReason: 'Excess amount paid by mistake — refunded to Nagad wallet on request', refundedAt: '2026-08-07T11:00', refundedBy: 'S. Chowdhury' }; st.payments.push(rf); audit('2026-08-07T11:00', 'S. Chowdhury', 'IDCOL', 'PAYMENT_REFUNDED', 'payment', rf.id, 'Success', 'Refunded', rf.refundReason); }
      if (h.profile === 'reversal') { var dup = { id: 'PAY-' + pad(paySeq++, 5), receiptNo: 'RCPT-260809-' + pad(paySeq, 4), systemId: h.systemId, daId: h.daId, date: '2026-08-09', amount: loan.installment, channel: 'bKash', reference: 'BK55019921', status: 'Reversed', allocated: [], recordedBy: 'Gateway (auto-post)', reconciled: true, reversalReason: 'Duplicate gateway posting (same reference)', reversedBy: 'S. Chowdhury', reversedAt: '2026-08-10T09:40' }; st.payments.push(dup); audit('2026-08-10T09:40', 'S. Chowdhury', 'IDCOL', 'PAYMENT_REVERSED', 'payment', dup.id, 'Success', 'Reversed', dup.reversalReason); }
      // telemetry
      var brand = byId(st.catalogue, h.brand).brand; var tel = { systemId: h.systemId, brand: brand, lastComm: today + 'T10:02', kwNow: 0, daily: [], cumulativeKwh: 0, faults: [], flags: [], strings: [{ v: 0, i: 0 }, { v: 0, i: 0 }], factor: 1 };
      var factor = h.profile === 'underperf' ? 0.58 : 0.82 + R() * 0.25; tel.factor = factor;
      var days = diffDays(today, h.installed); var cum = 0;
      for (var d = days; d >= 0; d--) { var date = addDays(today, -d); var base = h.kwp * 4.3 * factor * (0.85 + R() * 0.3); if (d === 0) base *= 0.42; if (h.profile === 'halted' && d < 3) base = 0; if (h.profile === 'offline' && d < 12) { continue; } var gen = Math.round(base * 10) / 10, cons = Math.round(gen * (0.45 + R() * 0.25) * 10) / 10, exp = h.profile === 'notexport' ? 0 : Math.round((gen - cons) * 10) / 10; cum += gen; tel.daily.push({ date: date, gen: gen, cons: cons, exp: Math.max(0, exp) }); }
      tel.cumulativeKwh = Math.round(cum); tel.kwNow = h.profile === 'halted' ? 0 : Math.round(h.kwp * factor * 0.71 * 100) / 100; tel.strings = [{ v: 318 + Math.round(R() * 20), i: Math.round((tel.kwNow / 2 / 0.33) * 100) / 100 }, { v: 315 + Math.round(R() * 20), i: Math.round((tel.kwNow / 2 / 0.33) * 100) / 100 }];
      if (h.profile === 'offline') { tel.lastComm = addDays(today, -12) + 'T17:41'; tel.kwNow = null; hh.inverterState = 'offline'; hh.offlineSince = addDays(today, -12); hh.pendingConnectivityHalt = true; notify('idcol', 'connectivity', h.systemId + ' out of network 12 days — connectivity halt pending (executes on reconnect).', addDays(today, -2) + 'T09:00'); }
      if (h.profile === 'fault') { tel.faults = [{ code: 'F-23 Isolation fault', at: addDays(today, -9) + 'T07:12' }, { code: 'F-23 Isolation fault', at: addDays(today, -5) + 'T06:58' }, { code: 'F-23 Isolation fault', at: addDays(today, -1) + 'T07:05' }]; }
      if (h.profile === 'halted') { hh.inverterState = 'halted'; hh.haltReason = 'overdue'; hh.haltedAt = addDays(today, -3) + 'T08:05'; st.commands.push({ id: 'CMD-' + pad(cmdSeq++, 4), ts: hh.haltedAt, systemId: h.systemId, command: 'HALT', trigger: 'overdue', operator: 'system (CRM rule)', result: 'ACK', latencyMs: 1840, brand: brand }); audit(hh.haltedAt, 'system', 'CRM', 'HALT_ISSUED', 'household', h.systemId, 'running', 'halted', 'Overdue 11 days > threshold 10'); notify('household:' + h.systemId, 'halted', 'System halted — overdue 11 days. Pay ' + money(loan.installment) + ' to resume.', hh.haltedAt, 'সিস্টেম বন্ধ — ১১ দিন বকেয়া। চালু করতে ' + money(loan.installment) + ' পরিশোধ করুন।'); notify('da:' + h.daId, 'halted', h.systemId + ' halted (overdue 11 days).', hh.haltedAt); loan.notified = { overdue3: true, pendingHalt3: true, halt3: true }; }
      if (h.profile === 'manualhistory') { st.commands.push({ id: 'CMD-' + pad(cmdSeq++, 4), ts: '2026-08-14T15:02', systemId: h.systemId, command: 'HALT', trigger: 'manual', operator: 'A. Karim', result: 'ACK', latencyMs: 2210, brand: brand, reason: 'Safety inspection after storm damage report' }, { id: 'CMD-' + pad(cmdSeq++, 4), ts: '2026-08-15T11:30', systemId: h.systemId, command: 'RESUME', trigger: 'manual', operator: 'A. Karim', result: 'ACK', latencyMs: 1950, brand: brand, reason: 'Site cleared by inspector' }); }
      if (h.profile === 'underperf') st.commands.push({ id: 'CMD-' + pad(cmdSeq++, 4), ts: addDays(today, -6) + 'T09:14', systemId: h.systemId, command: 'RESUME', trigger: 'overdue', operator: 'system (CRM rule)', result: 'FAILED', latencyMs: 30000, brand: brand, reason: 'Timeout — retried successfully 09:31' }, { id: 'CMD-' + pad(cmdSeq++, 4), ts: addDays(today, -6) + 'T09:31', systemId: h.systemId, command: 'RESUME', trigger: 'overdue', operator: 'system (CRM rule)', result: 'ACK', latencyMs: 2100, brand: brand });
      st.telemetry[h.systemId] = tel;
      // notifications for household
      notify('household:' + h.systemId, 'welcome', 'Welcome to the DRS Program. Your first installment of ' + money(loan.installment) + ' is due ' + fmtDate(commencement) + '.', h.installed + 'T12:00', 'ডিআরএস প্রোগ্রামে স্বাগতম। প্রথম কিস্তি ' + money(loan.installment) + ' প্রদেয় ' + fmtDate(commencement) + '।');
      if (h.profile === 'pending') notify('idcol', 'inspection', 'Inspection ' + insp.id + ' scheduled for ' + h.systemId + ' (' + h.district + ').', addDays(h.installed, 1) + 'T09:00');
    });
    // complaints
    st.complaints.push({ id: 'TKT-0012', systemId: 'DRS-CTG-0101', daId: 'DA-003', category: 'System not generating', description: 'Export to grid shows 0 since installation although generation is normal.', status: 'In progress', priority: 'Medium', assignedTo: 'DA', source: 'Household', createdAt: '2026-09-04T16:20', history: [{ at: '2026-09-04T16:20', by: 'Fatema Begum (household)', action: 'Submitted', note: 'Export shows 0 kWh' }, { at: '2026-09-05T10:05', by: 'SolarNet Bangladesh Ltd agent', action: 'In progress', note: 'Technician visit scheduled 12 Sep — checking net-meter CT wiring' }] });
    st.complaints.push({ id: 'TKT-0014', systemId: 'DRS-DHA-0004', daId: 'DA-001', category: 'Halt / suspension dispute', description: 'System halted but I paid at the branch on 9 Sep — please check.', status: 'Escalated to IDCOL', priority: 'High', assignedTo: 'IDCOL', source: 'Household', createdAt: '2026-09-09T18:40', history: [{ at: '2026-09-09T18:40', by: 'Rafiqul Islam (household)', action: 'Submitted', note: 'Paid cash at branch on 9 Sep' }, { at: '2026-09-10T09:15', by: 'Grameen Shakti Ltd agent', action: 'Escalated to IDCOL', note: 'No cash slip found for 9 Sep; household asked for slip copy. Restoration decision requires IDCOL authority.' }] });
    st.complaints.push({ id: 'TKT-0009', systemId: 'DRS-DHA-0002', daId: 'DA-001', category: 'Payment not posted', paymentRef: 'RCPT-260809', description: 'Paid twice via bKash by mistake on 9 Aug.', status: 'Resolved', priority: 'High', assignedTo: 'DA', source: 'Household', createdAt: '2026-08-09T18:05', resolution: 'Duplicate posting reversed 10 Aug (RCPT-260809).', history: [{ at: '2026-08-09T18:05', by: 'Shirin Akter (household)', action: 'Submitted', note: 'Paid twice by mistake' }, { at: '2026-08-10T09:40', by: 'S. Chowdhury (IDCOL)', action: 'Resolved', note: 'Duplicate posting reversed — reversal transaction recorded, original retained.' }] });
    // DA ledgers (refinance = sum of paid claims; advance = seeded outstanding)
    st.complaints.push({ id: 'TKT-0016', systemId: 'DRS-CUM-0088', daId: 'DA-003', category: 'Wrong payment / loan balance', paymentRef: 'RCPT-260906', description: 'My August installment shows unpaid although I paid via Nagad on 6 Sep.', status: 'Open', priority: 'High', assignedTo: 'DA', source: 'Household', attachment: 'nagad-screenshot.jpg', createdAt: '2026-09-10T20:12', history: [{ at: '2026-09-10T20:12', by: 'Sultana Razia (household)', action: 'Submitted', note: 'Paid via Nagad on 6 Sep' }] });
    st.complaints.push({ id: 'TKT-0017', systemId: 'DRS-KHU-0202', daId: 'DA-002', category: 'System not generating', description: 'Generation has dropped by half since last week.', status: 'Awaiting IDCOL', priority: 'Medium', assignedTo: 'IDCOL', source: 'DA', createdAt: '2026-09-10T11:30', history: [{ at: '2026-09-10T11:30', by: 'BRAC Solar Programme agent', action: 'Submitted', note: 'Technician found no wiring fault; suspect panel degradation — requesting IDCOL technical review' }] });
    // pending bank transfers: one without a bank record (exception), one with an amount mismatch
    st.payments.push({ id: 'PAY-' + pad(paySeq++, 5), receiptNo: null, systemId: 'DRS-KHU-0201', daId: 'DA-002', date: addDays(today, -2), ts: addDays(today, -2) + 'T15:40', amount: loanOf(st, 'DRS-KHU-0201').installment, channel: 'Bank', bank: 'Sonali Bank', reference: 'NPSB55120099', status: 'Pending verification', stage: 'pending', stages: [{ stage: 'Submitted', at: addDays(today, -2) + 'T15:40' }, { stage: 'Pending verification', at: addDays(today, -2) + 'T15:40' }], allocated: [], recordedBy: 'Nasima Khatun (household)', reconciled: false, exception: 'Missing bank entry' });
    st.exceptions.push({ id: 'EXC-' + pad(1, 4), paymentId: st.payments[st.payments.length - 1].id, systemId: 'DRS-KHU-0201', daId: 'DA-002', type: 'Missing bank entry', detail: 'No bank record with reference NPSB55120099 in the DA Program account statement (2 days)', owner: 'Accounts & Finance', status: 'Open', createdAt: addDays(today, -1) + 'T07:30' });
    st.payments.push({ id: 'PAY-' + pad(paySeq++, 5), receiptNo: null, systemId: 'DRS-CTG-0101', daId: 'DA-003', date: addDays(today, -1), ts: addDays(today, -1) + 'T09:05', amount: loanOf(st, 'DRS-CTG-0101').installment, channel: 'Bank', bank: 'DA Program account', reference: 'DEP-771204', status: 'Pending verification', stage: 'pending', stages: [{ stage: 'Submitted', at: addDays(today, -1) + 'T09:05' }, { stage: 'Pending verification', at: addDays(today, -1) + 'T09:05' }], allocated: [], recordedBy: 'SolarNet Bangladesh Ltd agent', reconciled: false, exception: 'Amount mismatch' });
    st.bankStatement.push({ id: 'BNK-' + pad(1, 4), date: addDays(today, -1), reference: 'DEP-771204', amount: 5000, account: 'DA Program account DA-003', systemId: 'DRS-CTG-0101', matchedPaymentId: null });
    st.exceptions.push({ id: 'EXC-' + pad(2, 4), paymentId: st.payments[st.payments.length - 1].id, systemId: 'DRS-CTG-0101', daId: 'DA-003', type: 'Amount mismatch', detail: 'Bank ' + money(5000) + ' vs CRM ' + money(loanOf(st, 'DRS-CTG-0101').installment) + ' for reference DEP-771204', owner: 'Accounts & Finance', status: 'Open', createdAt: addDays(today, -1) + 'T07:30' });
    st.exceptions.push({ id: 'EXC-' + pad(3, 4), paymentId: (st.payments.filter(function (p) { return p.status === 'Reversed'; })[0] || {}).id, systemId: 'DRS-DHA-0002', daId: 'DA-001', type: 'Duplicate', detail: 'Same gateway reference BK55019921 posted twice on 9 Aug', owner: 'Accounts & Finance', status: 'Resolved', createdAt: '2026-08-10T07:30', resolvedBy: 'S. Chowdhury', resolvedAt: '2026-08-10T09:40', resolution: 'Duplicate posting reversed (REV entry recorded; original retained)' });
    st.exceptions.push({ id: 'EXC-' + pad(4, 4), paymentId: (st.payments.filter(function (p) { return p.status === 'Failed'; })[0] || {}).id, systemId: 'DRS-DHA-0001', daId: 'DA-001', type: 'Failed', detail: 'PSP timeout — no debit; household retried successfully', owner: 'Accounts & Finance', status: 'Resolved', createdAt: '2026-08-15T07:30', resolvedBy: 'S. Chowdhury', resolvedAt: '2026-08-15T10:00', resolution: 'No financial impact — closed' });
    st.exceptions.push({ id: 'EXC-' + pad(5, 4), paymentId: (st.payments.filter(function (p) { return p.status === 'Refunded'; })[0] || {}).id, systemId: 'DRS-DHA-0003', daId: 'DA-001', type: 'Refunded', detail: 'Excess payment refunded to Nagad wallet on request', owner: 'Accounts & Finance', status: 'Resolved', createdAt: '2026-08-07T07:30', resolvedBy: 'S. Chowdhury', resolvedAt: '2026-08-07T11:00', resolution: 'Refund entry recorded' });
    st.exceptions.forEach(function (e, i) { e.id = 'EXC-' + pad(i + 1, 4); });
    st.seq = 1100;
    st.das.forEach(function (da) { rebuildDaLedgers(da.id, st); });
    // seed audit for admin actions
    audit('2026-05-15T10:00', 'A. Karim', 'IDCOL', 'DA_CREATED', 'da', 'DA-001', null, { name: 'Grameen Shakti Ltd', limit: 500000000 });
    audit('2026-05-22T10:00', 'A. Karim', 'IDCOL', 'DA_CREATED', 'da', 'DA-002', null, { name: 'BRAC Solar Programme', limit: 400000000 });
    audit('2026-06-03T10:00', 'A. Karim', 'IDCOL', 'DA_CREATED', 'da', 'DA-003', null, { name: 'SolarNet Bangladesh Ltd', limit: 250000000 });
    st.batches.forEach(function (b) { if (b.status === 'Verified') audit(b.verifiedAt + 'T14:00', 'M. Hasan', 'IDCOL', 'BATCH_ALLOTTED', 'batch', b.id, 'Pending', 'Verified', b.quantity + ' serials → ' + b.daId); });
    audit('2026-07-14T16:00', 'TSC', 'IDCOL', 'CATALOGUE_STATUS', 'catalogue', 'CAT-I05', 'active', 'suspended', 'Read-only API — no control channel; suspended pending OEM agreement');
    audit('2026-09-01T09:30', 'T. Ahmed', 'IDCOL', 'SETTINGS_CHANGED', 'settings', 'haltOverdueDays', 7, 10, 'Aligned with §3.1 of business requirement');
    st.audit.sort(function (a, b) { return a.ts < b.ts ? -1 : 1; });
    notify('idcol', 'ews', 'Early warning: SolarNet Bangladesh Ltd PaR30 above threshold; Bank Guarantee expires 15 Nov 2026.', '2026-09-10T08:00');
    notify('idcol', 'claim', '2 refinance claims prepared and awaiting checker approval.', '2026-09-10T17:10');
    notify('da:DA-003', 'security', 'Your Bank Guarantee expires 15 Nov 2026 — renewal required within notice period.', '2026-09-10T08:00');
    // two prepared claims for the demo (so "awaiting approval" is non-zero)
    ['DRS-CUM-0088', 'DRS-JSR-0055'].forEach(function (sid) { var c = byId(st.claims, sid, 'systemId'); c.status = 'Under review'; c.paidAt = null; c.approvedAt = null; c.checker = null; c.submittedBy = byId(st.das, c.daId).name + ' agent'; c.submittedAt = '2026-09-10T11:20'; c.preparedAt = '2026-09-10T16:40'; c.docs = ['Household contract', 'DA field report', 'IDCOL inspection report', 'Equipment serial list']; });
    st.das.forEach(function (da) { rebuildDaLedgers(da.id, st); });
    return st;
  }

  function loanOf(st, sid) { return byId(st.loans, sid, 'systemId'); }
  function rebuildDaLedgers(daId, st) {
    st = st || S; var da = byId(st.das, daId); if (!da) return;
    var paid = st.claims.filter(function (c) { return c.daId === daId && c.status === 'Paid'; });
    var disbursed = paid.reduce(function (a, c) { return a + c.amount; }, 0);
    var first = paid.length ? paid.map(function (c) { return c.paidAt; }).sort()[0] : null;
    var ref = { daId: daId, type: 'Refinance', sanctioned: da.facility.limit, disbursed: disbursed, headroom: da.facility.limit - disbursed, rate: da.facility.rate, tenorMonths: da.facility.tenorMonths, commencement: first ? addMonths(first, da.facility.graceMonths) : null, schedule: [] };
    if (disbursed > 0) { var s = buildSchedule(disbursed, da.facility.rate, da.facility.tenorMonths, ref.commencement); ref.installment = s.installment; ref.schedule = s.rows; ref.schedule.forEach(function (r) { if (r.dueDate <= st.sim.today) { r.paid = r.amount; r.paidAt = r.dueDate; } }); }
    ref.outstanding = ref.schedule.reduce(function (a, r) { return a + (r.amount - r.paid); }, 0);
    var adv = { daId: daId, type: 'Advance', sanctioned: da.advance.limit, disbursed: da.advance.outstanding, outstanding: da.advance.outstanding, headroom: da.advance.limit - da.advance.outstanding, rate: da.advance.rate, tenorMonths: da.advance.tenorMonths, commencement: addMonths(da.onboardedAt, 1), schedule: [] };
    if (adv.disbursed > 0) { var s2 = buildSchedule(adv.disbursed, adv.rate, adv.tenorMonths, adv.commencement); adv.installment = s2.installment; adv.schedule = s2.rows; adv.schedule.forEach(function (r) { if (r.dueDate <= st.sim.today) { r.paid = r.amount; r.paidAt = r.dueDate; } }); adv.outstanding = adv.schedule.reduce(function (a, r) { return a + (r.amount - r.paid); }, 0); }
    st.daLedgers = st.daLedgers.filter(function (l) { return l.daId !== daId; }); st.daLedgers.push(ref, adv);
  }

  /* ---------- persistence ---------- */
  function load() { if (S) return S; try { var raw = store.getItem(KEY); if (raw) { var parsed = JSON.parse(raw); if (parsed && parsed.version === SEED_VERSION) S = parsed; } } catch (e) { S = null; } if (!S) { S = seed(); persist(false); } return S; }
  var listeners = [];
  function persist(notify) { try { store.setItem(KEY, JSON.stringify(S)); } catch (e) { } if (notify !== false) emit({ remote: false }); }
  function emit(detail) { listeners.forEach(function (fn) { try { fn(S, detail); } catch (e) { if (typeof console !== 'undefined') console.error(e); } }); if (hasWindow && window.dispatchEvent) window.dispatchEvent(new CustomEvent('drs:change', { detail: detail })); }
  function onChange(fn) { listeners.push(fn); }
  if (hasWindow) window.addEventListener('storage', function (e) { if (e.key === KEY && e.newValue) { try { S = JSON.parse(e.newValue); emit({ remote: true }); } catch (err) { } } });
  function reset() { S = seed(); persist(); return S; }

  /* ---------- audit / notify / helpers on live state ---------- */
  function audit(actor, role, action, entity, entityId, before, after, reason) { S.audit.push({ id: uid('AUD'), ts: tick(), actor: actor, role: role, action: action, entity: entity, entityId: entityId, before: before == null ? null : before, after: after == null ? null : after, reason: reason || null }); }
  function notify(audience, type, message, bn) { S.notifications.push({ id: uid('NTF'), audience: audience, type: type, message: message, messageBn: bn || null, createdAt: nowTs(), read: false }); }
  function markChanged() { var a = Array.prototype.slice.call(arguments); S.sim.lastChanged = a; }
  function da(id) { return byId(S.das, id); }
  function household(id) { return byId(S.households, id, 'systemId'); }
  function loan(id) { return byId(S.loans, id, 'systemId'); }
  function cat(id) { return byId(S.catalogue, id); }
  function pkgOf(kwp) { return byId(S.settings.packages, +kwp, 'kwp'); }
  function serialsFor(systemId) { return S.inventory.filter(function (u) { return u.systemId === systemId; }); }

  /* ---------- loan math ---------- */
  function balances(systemId) {
    var l = loan(systemId); if (!l) return null; var today = S.sim.today;
    var due = 0, paid = 0, overdue = 0, overdueDays = 0, overdueCount = 0, remaining = 0, principalPaid = 0, nextRow = null;
    l.schedule.forEach(function (r) {
      remaining += r.amount - r.paid; paid += r.paid;
      if (r.paid >= r.amount) principalPaid += r.principal; else if (r.paid > 0) principalPaid += Math.round(r.principal * r.paid / r.amount);
      if (r.dueDate <= today) { due += r.amount; if (r.paid < r.amount) { var od = r.amount - r.paid; overdue += od; overdueCount++; var days = diffDays(today, r.dueDate); if (days > overdueDays) overdueDays = days; } }
      if (!nextRow && r.paid < r.amount) nextRow = r;
    });
    return { due: due, paid: paid, overdue: overdue, overdueDays: overdueDays, overdueCount: overdueCount, remaining: remaining, principalOutstanding: l.daFinanced - principalPaid, credit: l.credit || 0, next: nextRow, paidCount: l.schedule.filter(function (r) { return r.paid >= r.amount; }).length, collectionEfficiency: due ? Math.min(100, Math.round(Math.min(paid, due) / due * 1000) / 10) : 100 };
  }
  function rowStatus(r) { var today = S.sim.today; if (r.paid >= r.amount) return 'Paid'; if (r.paid > 0) return r.dueDate < today ? 'Overdue (partial)' : 'Partial'; if (r.dueDate < today) return 'Overdue'; if (r.dueDate === today) return 'Due today'; return 'Upcoming'; }

  /* ---------- actions ---------- */
  function onboardDA(form, actor) {
    var n = S.das.length + 1, id = form.id || ('DA-' + pad(n, 3));
    if (byId(S.das, id)) return byId(S.das, id);
    var d = makeDA({ id: id, name: form.name, legalForm: form.legalForm, registrationNo: form.registrationNo, districts: form.districts, onboardedAt: S.sim.today, keyPersons: form.keyPersons || [], highlights: form.highlights || [{ year: 2023, revenue: 0, assets: 0, netIncome: 0 }, { year: 2024, revenue: 0, assets: 0, netIncome: 0 }, { year: 2025, revenue: 0, assets: 0, netIncome: 0 }], solarTrackRecord: form.solarTrackRecord || '—', limit: +form.limit, advanceLimit: +form.advanceLimit, bg: +form.bg, bgBank: form.bgBank, bgExpiry: form.bgExpiry, dsra: +form.dsra || 0, pg: form.pg || [], cg: form.cg || [] });
    S.das.push(d); rebuildDaLedgers(id);
    audit(actor || 'A. Karim', 'IDCOL', 'DA_CREATED', 'da', id, null, { name: d.name, legalForm: d.legalForm, limit: d.facility.limit, advanceLimit: d.advance.limit, bg: d.security.bgAmount });
    notify('idcol', 'da', 'DA ' + d.name + ' onboarded — refinance limit ' + money(d.facility.limit, { short: true }) + ', advance ' + money(d.advance.limit, { short: true }) + '.');
    notify('da:' + id, 'welcome', 'Welcome to the DRS Program. Your facilities are active. Declare your first equipment batches to start onboarding households.');
    S.sim.activeDaId = id; markChanged('idcol', 'da'); persist(); return d;
  }
  function declareBatch(daId, form, actor) {
    var id = form.id || uid('B'); var c = cat(form.catalogueId); if (!c || c.status !== 'active') throw new Error('Model not active in Approved Equipment Catalogue');
    var serials = form.serials && form.serials.length ? form.serials : []; var rejected = []; var seen = {};
    serials = serials.filter(function (sn) { sn = String(sn).trim(); if (!sn) return false; if (seen[sn] || S.inventory.some(function (u) { return u.serial === sn; })) { rejected.push(sn); return false; } seen[sn] = true; return true; });
    if (!serials.length) for (var i = 1; i <= +form.quantity; i++) serials.push(c.prefix + '-' + id.replace('B-', '') + '-' + pad(i));
    var b = { id: id, daId: daId, catalogueId: c.id, quantity: serials.length, supplier: form.supplier, psiRef: form.psiRef || ('PSI-' + id.replace('B-', '') + '-2026'), psiUploadedAt: S.sim.today, status: 'Pending', verifiedBy: null, verifiedAt: null, serials: serials, financedAmount: 0, rejectedSerials: rejected };
    S.batches.push(b); audit(actor || 'DA agent', 'DA', 'BATCH_DECLARED', 'batch', id, null, { model: c.brand + ' ' + c.model, quantity: b.quantity, rejectedDuplicates: rejected.length });
    notify('idcol', 'batch', 'PSI batch ' + id + ' (' + c.brand + ' ' + c.model + ' × ' + b.quantity + ') declared by ' + da(daId).name + ' — verification required.');
    markChanged('idcol', 'da'); persist(); return b;
  }
  function verifyBatch(batchId, officer, decision, reason) {
    var b = byId(S.batches, batchId); if (!b || b.status !== 'Pending') return b;
    if (decision === 'Rejected') { b.status = 'Rejected'; b.verifiedBy = officer; b.verifiedAt = S.sim.today; b.reason = reason; audit(officer, 'IDCOL', 'BATCH_REJECTED', 'batch', b.id, 'Pending', 'Rejected', reason); markChanged('idcol', 'da'); persist(); return b; }
    b.status = 'Verified'; b.verifiedBy = officer || 'M. Hasan'; b.verifiedAt = S.sim.today;
    b.serials.forEach(function (sn) { S.inventory.push({ serial: sn, batchId: b.id, daId: b.daId, catalogueId: b.catalogueId, status: 'in_stock', systemId: null, updatedAt: S.sim.today }); });
    var c = cat(b.catalogueId); var unit = c.category === 'panel' ? 14500 : c.category === 'inverter' ? 62000 : 118000; b.financedAmount = unit * b.quantity;
    audit(b.verifiedBy, 'IDCOL', 'BATCH_ALLOTTED', 'batch', b.id, 'Pending', 'Verified', b.quantity + ' serials → ' + b.daId + ' inventory');
    notify('da:' + b.daId, 'batch', 'Batch ' + b.id + ' verified — ' + b.quantity + ' × ' + c.brand + ' ' + c.model + ' added to your inventory (in stock).');
    markChanged('idcol', 'da'); persist(); return b;
  }
  function stockFor(daId, catId) { return S.inventory.filter(function (u) { return u.daId === daId && u.catalogueId === catId && u.status === 'in_stock'; }); }
  function daModels(daId, category) { var seen = {}; S.inventory.forEach(function (u) { if (u.daId === daId && u.status === 'in_stock') { var c = cat(u.catalogueId); if (c.category === category) seen[c.id] = (seen[c.id] || 0) + 1; } }); return Object.keys(seen).map(function (id) { return { cat: cat(id), inStock: seen[id] }; }); }
  function onboardHousehold(daId, form, actor) {
    var p = pkgOf(form.kwp); if (!p) throw new Error('Invalid package — choose a configured package');
    var dA = da(daId); if (!dA || dA.status !== 'Active') throw new Error('DA is not active');
    ['name', 'nid', 'phone', 'district', 'upazila', 'address'].forEach(function (k) { if (!form[k] || !String(form[k]).trim()) throw new Error('Required household information missing: ' + k); });
    if (!/^\d{10}$|^\d{13}$|^\d{17}$/.test(String(form.nid).replace(/\s/g, ''))) throw new Error('NID must be 10, 13 or 17 digits');
    if (!form.gps || isNaN(+form.gps.lat) || isNaN(+form.gps.lng)) throw new Error('Site GPS is required');
    if (dA.districts.indexOf(form.district) < 0) throw new Error('District ' + form.district + ' is outside this DA\'s authorised areas (' + dA.districts.join(', ') + ')');
    var dup = S.households.filter(function (h) { return h.nid === form.nid; })[0]; if (dup) throw new Error('Duplicate household / financing control: NID already linked to system ' + dup.systemId);
    if (!form.daReport || !form.daReport.installer || form.daReport.checklistComplete === false || (form.daReport.photos || 0) < 6) throw new Error('DA field report incomplete: checklist, 6 photos and installer sign-off are required');
    if (form.refinance != null && +form.refinance > p.maxRefinance) throw new Error('Refinance requested ' + money(form.refinance) + ' exceeds package maximum ' + money(p.maxRefinance));
    if (+form.contractPrice > p.ceiling) throw new Error('Contract price ' + money(form.contractPrice) + ' exceeds ceiling ' + money(p.ceiling) + ' for ' + p.kwp + ' kWp package');
    var allSerials = form.panelSerials.concat([form.inverterSerial, form.batterySerial]);
    var seen = {}; allSerials.forEach(function (sn) { if (!sn) throw new Error("Select every required equipment serial from this DA's in-stock inventory"); if (seen[sn]) throw new Error('Serial ' + sn + ' selected twice'); seen[sn] = 1; });
    var units = allSerials.map(function (sn) { var u = byId(S.inventory, sn, 'serial'); if (!u) throw new Error('Serial ' + sn + ' not found in inventory'); if (u.daId !== daId) throw new Error('Serial ' + sn + ' belongs to another DA'); if (u.systemId) throw new Error('Serial ' + sn + ' is already assigned to ' + u.systemId); if (u.status !== 'in_stock' && u.status !== 'reserved') throw new Error('Serial ' + sn + ' is not available (' + u.status + ')'); return u; });
    if (form.panelSerials.length !== p.panels) throw new Error(p.kwp + ' kWp package requires ' + p.panels + ' panels');
    if (!form.daReport || !form.daReport.installer) throw new Error('DA field inspection report with installer sign-off is mandatory');
    var code = DISTRICT_CODE[form.district] || form.district.slice(0, 3).toUpperCase(); var seqN = S.households.filter(function (h) { return h.systemId.indexOf('DRS-' + code) === 0; }).length + 200; var systemId = form.systemId || ('DRS-' + code + '-' + pad(seqN + 13));
    while (household(systemId)) systemId = 'DRS-' + code + '-' + pad(++seqN + 13);
    units.forEach(function (u) { u.status = 'installed'; u.systemId = systemId; u.updatedAt = S.sim.today; });
    var hh = { systemId: systemId, daId: daId, name: form.name, nid: form.nid, phone: form.phone, district: form.district, upazila: form.upazila, address: form.address, gps: form.gps, packageKwp: p.kwp, contractPrice: +form.contractPrice, benchmark: p.benchmark, ceiling: p.ceiling, panelSerials: form.panelSerials.slice(), inverterSerial: form.inverterSerial, batterySerial: form.batterySerial, inverterCat: byId(S.inventory, form.inverterSerial, 'serial').catalogueId, panelCat: byId(S.inventory, form.panelSerials[0], 'serial').catalogueId, batteryCat: byId(S.inventory, form.batterySerial, 'serial').catalogueId, installationDate: S.sim.today, netMetering: 'Applied', daReport: { checklistOk: true, photos: form.daReport.photos || 6, installer: form.daReport.installer, uploadedAt: S.sim.today }, status: 'Active', inverterState: 'running', haltReason: null, haltedAt: null, offlineSince: null, pendingConnectivityHalt: false, createdAt: nowTs() };
    S.households.push(hh);
    var upfront = Math.round(hh.contractPrice * 0.2), financed = hh.contractPrice - upfront, refinanced = Math.min(p.maxRefinance, +form.refinance || Math.round(financed * 0.88));
    var commencement = addMonths(S.sim.today, 1), sch = buildSchedule(financed, S.settings.rate, S.settings.tenorMonths, commencement);
    S.loans.push({ systemId: systemId, daId: daId, totalCost: hh.contractPrice, upfront: upfront, daFinanced: financed, idcolRefinanced: refinanced, rate: S.settings.rate, tenorMonths: S.settings.tenorMonths, installment: sch.installment, commencement: commencement, maturity: addMonths(commencement, S.settings.tenorMonths - 1), schedule: sch.rows, credit: 0, notified: {} });
    var insp = { id: uid('INS'), systemId: systemId, daId: daId, inspector: 'R. Sultana', scheduledAt: addDays(S.sim.today, 2), completedAt: null, checklist: CHECKLIST.map(function (c) { return { item: c, result: 'Pending' }; }), scanned: { panels: [], inverter: null, battery: null }, mismatch: null, gpsDistanceM: null, photos: 0, result: 'Pending', reason: null, log: [] };
    S.inspections.push(insp);
    var claim = { id: uid('CLM'), daId: daId, systemId: systemId, amount: refinanced, packageMax: p.maxRefinance, status: 'Not eligible', maker: null, checker: null, preparedAt: null, approvedAt: null, paidAt: null, holdReason: null, duplicateCheck: 'Clear' };
    S.claims.push(claim);
    var brand = cat(hh.inverterCat).brand; S.telemetry[systemId] = { systemId: systemId, brand: brand, lastComm: nowTs(), kwNow: Math.round(p.kwp * 0.62 * 100) / 100, daily: [{ date: S.sim.today, gen: 0, cons: 0, exp: 0 }], cumulativeKwh: 0, faults: [], flags: [], strings: [{ v: 322, i: 3.8 }, { v: 319, i: 3.7 }], factor: 0.95 };
    audit(actor || 'DA agent', 'DA', 'HOUSEHOLD_ONBOARDED', 'household', systemId, null, { name: hh.name, package: p.kwp + ' kWp', serials: allSerials.length, contract: hh.contractPrice });
    allSerials.forEach(function (sn) { audit(actor || 'DA agent', 'DA', 'SERIAL_INSTALLED', 'inventory', sn, 'in_stock', 'installed', 'Linked to ' + systemId); });
    notify('idcol', 'inspection', 'New system ' + systemId + ' onboarded by ' + da(daId).name + ' — IDCOL inspection ' + insp.id + ' scheduled ' + fmtDate(insp.scheduledAt) + '.');
    notify('household:' + systemId, 'welcome', 'Welcome to the DRS Program, ' + hh.name + '. Your first installment of ' + money(sch.installment) + ' is due ' + fmtDate(commencement) + '.', 'ডিআরএস প্রোগ্রামে স্বাগতম। প্রথম কিস্তি ' + money(sch.installment) + ' প্রদেয় ' + fmtDate(commencement) + '।');
    S.sim.activeSystemId = systemId; S.sim.activeDaId = daId; markChanged('idcol', 'da', 'household'); persist(); return hh;
  }
  function recordInspection(inspId, form, actor) {
    var insp = byId(S.inspections, inspId); if (!insp) return null; var hh = household(insp.systemId); var claim = byId(S.claims, insp.systemId, 'systemId');
    insp.scanned = form.scanned; insp.inspector = actor || insp.inspector; insp.photos = form.photos || 6; insp.gpsDistanceM = form.gpsDistanceM == null ? 18 : form.gpsDistanceM;
    var mism = []; hh.panelSerials.forEach(function (sn, i) { if ((form.scanned.panels[i] || '') !== sn) mism.push('Panel ' + (i + 1)); }); if (form.scanned.inverter !== hh.inverterSerial) mism.push('Inverter'); if (form.scanned.battery !== hh.batterySerial) mism.push('Battery');
    insp.mismatch = mism.length > 0; insp.mismatches = mism;
    insp.checklist.forEach(function (c, i) { c.result = form.checklist && form.checklist[i] ? form.checklist[i] : 'Pass'; });
    var result = form.result || (insp.mismatch ? 'Exception' : 'Pass'); var before = insp.result; insp.result = result; insp.reason = form.reason || (insp.mismatch ? 'Serial mismatch: ' + mism.join(', ') : null); insp.completedAt = tick();
    insp.log.push({ ts: insp.completedAt, user: insp.inspector, action: before + ' → ' + result, reason: insp.reason || 'Checklist complete, serials match' });
    if (result === 'Pass') { claim.status = 'Eligible'; claim.holdReason = null; notify('da:' + hh.daId, 'inspection', 'Inspection ' + insp.id + ' passed for ' + hh.systemId + ' — refinance claim ' + claim.id + ' is eligible (' + money(claim.amount) + ').'); notify('household:' + hh.systemId, 'inspection', 'IDCOL inspection of your system completed — Pass.', 'আইডিকল পরিদর্শন সম্পন্ন — উত্তীর্ণ।'); }
    else if (result === 'Exception') { claim.status = 'On hold'; claim.holdReason = insp.reason; notify('da:' + hh.daId, 'inspection', 'Inspection ' + insp.id + ' raised an exception for ' + hh.systemId + ' — claim on hold: ' + insp.reason); }
    else { claim.status = 'Not eligible'; claim.holdReason = insp.reason; notify('da:' + hh.daId, 'inspection', 'Inspection ' + insp.id + ' failed for ' + hh.systemId + '.'); }
    audit(insp.inspector, 'IDCOL', 'INSPECTION_' + result.toUpperCase(), 'inspection', insp.id, before, result, insp.reason);
    audit('system', 'Inspection', 'CLAIM_STATUS', 'claim', claim.id, 'Not eligible', claim.status, 'Inspection-based eligibility determination');
    S.sim.activeSystemId = hh.systemId; markChanged('idcol', 'da', 'household'); persist(); return insp;
  }
  function claimAction(claimId, action, actor, reason, role) {
    var c = byId(S.claims, claimId); if (!c) return null; var before = c.status; actor = actor || 'A. Karim'; role = role || 'IDCOL';
    if (role === 'DA' && ['prepare', 'review', 'approve', 'disburse', 'hold', 'release', 'reject'].indexOf(action) >= 0) throw new Error('Not permitted: "' + action + '" requires IDCOL authority (maker-checker-approver). A DA can prepare and submit its claim only.');
    var insp = S.inspections.filter(function (i) { return i.systemId === c.systemId; })[0];
    if (action === 'draft') { if (c.status !== 'Eligible' && c.status !== 'Rejected') throw new Error('A claim can only be prepared once the IDCOL inspection has passed (status Eligible) — current status: ' + c.status); if (!insp || insp.result !== 'Pass') throw new Error('IDCOL inspection has not passed for ' + c.systemId); if (S.claims.some(function (o) { return o.id !== c.id && o.systemId === c.systemId && ['Submitted', 'Under review', 'Approved', 'Paid'].indexOf(o.status) >= 0; })) throw new Error('Duplicate refinancing: ' + c.systemId + ' already has an active claim'); var l = loan(c.systemId); c.amount = Math.min(c.packageMax, Math.round(l.daFinanced * 0.88)); c.status = 'Draft'; c.draftedBy = actor; c.draftedAt = tick(); c.docs = ['Household contract', 'DA field report', 'IDCOL inspection ' + insp.id, 'Equipment serial list']; c.holdReason = null; }
    else if (action === 'submit') { if (c.status !== 'Draft') throw new Error('Only a Draft claim can be submitted'); if (c.amount > c.packageMax) throw new Error('Refinance amount exceeds package maximum'); c.status = 'Submitted'; c.submittedBy = actor; c.submittedAt = tick(); notify('idcol', 'claim', 'Refinance claim ' + c.id + ' (' + c.systemId + ', ' + money(c.amount) + ') submitted by ' + da(c.daId).name + ' — review required.'); }
    else if (action === 'prepare' || action === 'review') { if (c.status !== 'Submitted') throw new Error('Claim must be Submitted by the DA before IDCOL review'); c.status = 'Under review'; c.maker = actor; c.preparedAt = tick(); }
    else if (action === 'approve') { if (c.status !== 'Under review') throw new Error('Claim must be Under review'); if (c.maker === actor) throw new Error('Maker-checker: the checker must be a different user from the reviewer (' + c.maker + ')'); c.status = 'Approved'; c.checker = actor; c.approvedAt = tick(); notify('da:' + c.daId, 'claim', 'Refinance claim ' + c.id + ' approved by IDCOL — awaiting disbursement.'); }
    else if (action === 'reject') { if (['Submitted', 'Under review', 'On hold'].indexOf(c.status) < 0) throw new Error('Claim cannot be rejected in status ' + c.status); c.status = 'Rejected'; c.holdReason = reason; notify('da:' + c.daId, 'claim', 'Refinance claim ' + c.id + ' rejected by IDCOL: ' + reason); }
    else if (action === 'disburse') { if (c.status !== 'Approved') throw new Error('Claim must be Approved'); c.status = 'Paid'; c.paidAt = S.sim.today; c.disbursedBy = actor; rebuildDaLedgers(c.daId); notify('da:' + c.daId, 'claim', 'Refinance claim ' + c.id + ' paid — ' + money(c.amount) + ' disbursed to your Program account.'); }
    else if (action === 'hold') { c.status = 'On hold'; c.holdReason = reason; }
    else if (action === 'release') { if (c.status !== 'On hold') throw new Error('Claim is not on hold'); c.status = 'Eligible'; c.holdReason = null; }
    var CLAIM_EVENTS = { draft: 'CLAIM_PREPARE', submit: 'CLAIM_SUBMITTED', prepare: 'CLAIM_REVIEWED', review: 'CLAIM_REVIEWED', approve: 'CLAIM_APPROVED', reject: 'CLAIM_REJECTED', hold: 'CLAIM_HOLD', release: 'CLAIM_RELEASED', disburse: 'CLAIM_DISBURSED' };
    if (action === 'prepare' || action === 'review') c.reviewer = actor;
    audit(actor, role, CLAIM_EVENTS[action] || ('CLAIM_' + action.toUpperCase()), 'claim', c.id, before, c.status, reason);
    markChanged('idcol', 'da'); persist(); return c;
  }
  function recordPayment(systemId, form, actor) { var p = postPayment(systemId, form, actor, {}); S.sim.activeSystemId = systemId; markChanged('idcol', 'da', 'household'); persist(); return p; }
  function postPayment(systemId, form, actor, opts) {
    var l = loan(systemId); var hh = household(systemId); if (!l) throw new Error('No loan for ' + systemId); var amount = Math.round(+form.amount); if (!(amount > 0)) throw new Error('Amount must be positive');
    var remaining = amount + (l.credit || 0); l.credit = 0; var alloc = [];
    l.schedule.forEach(function (r) { if (remaining <= 0) return; var open = r.amount - r.paid; if (open <= 0) return; var take = Math.min(open, remaining); r.paid += take; remaining -= take; if (r.paid >= r.amount) r.paidAt = S.sim.today; alloc.push({ n: r.n, amount: take }); });
    if (remaining > 0) l.credit = remaining;
    var ch = form.channel || 'bKash'; var ref = form.reference || ((ch === 'bKash' ? 'BK' : ch === 'Nagad' ? 'NG' : ch === 'Card' ? 'CD' : ch === 'Bank' ? 'NPSB' : 'CASH') + Math.floor(1e7 + Math.random() * 9e7));
    var p = { id: uid('PAY'), receiptNo: 'RCPT-' + S.sim.today.replace(/-/g, '').slice(2) + '-' + pad(S.seq, 4), systemId: systemId, daId: l.daId, date: S.sim.today, ts: tick(), amount: amount, channel: ch, reference: ref, status: 'Success', allocated: alloc, recordedBy: actor || (ch === 'Cash' || ch === 'Bank' ? 'DA collection agent' : 'Gateway (auto-post)'), reconciled: ch === 'Cash' ? false : true, credit: l.credit };
    S.payments.push(p);
    audit(p.recordedBy, ch === 'Cash' || ch === 'Bank' ? 'DA' : 'Gateway', 'PAYMENT_POSTED', 'payment', p.id, null, { amount: amount, channel: ch, reference: ref, allocated: alloc.map(function (a) { return '#' + a.n; }).join(',') });
    notify('household:' + systemId, 'payment_received', 'Payment of ' + money(amount) + ' received via ' + ch + '. Receipt ' + p.receiptNo + '.', money(amount) + ' ' + ch + ' এর মাধ্যমে গৃহীত। রসিদ ' + p.receiptNo + '।');
    notify('da:' + l.daId, 'payment_received', money(amount) + ' received from ' + hh.name + ' (' + systemId + ') via ' + ch + '.');
    var b = balances(systemId);
    if (hh.inverterState === 'halted' && hh.haltReason === 'overdue' && b.overdue === 0) { issueCommand(systemId, 'RESUME', 'overdue', 'system (CRM rule)', 'Overdue cleared by payment ' + p.receiptNo); }
    if (b.overdue === 0) { l.notified = {}; }
    return p;
  }
  function submitBankReceipt(systemId, form, actor) {
    var l = loan(systemId); var hh = household(systemId); var amount = Math.round(+form.amount); if (!(amount > 0)) throw new Error('Amount must be positive'); if (!form.reference) throw new Error('Transfer reference is required');
    if (form.bankRecord !== false) { S.bankStatement = S.bankStatement || []; S.bankStatement.push({ id: uid('BNK'), date: S.sim.today, reference: form.reference, amount: form.bankAmount != null ? Math.round(+form.bankAmount) : amount, account: 'DA Program account ' + l.daId, systemId: systemId, matchedPaymentId: null }); }
    var p = { id: uid('PAY'), receiptNo: null, systemId: systemId, daId: l.daId, date: S.sim.today, ts: tick(), amount: amount, channel: 'Bank', bank: form.bank || null, reference: form.reference, status: 'Pending verification', stage: 'pending', stages: [{ stage: 'Submitted', at: nowTs() }, { stage: 'Pending verification', at: nowTs() }], allocated: [], recordedBy: actor || (hh.name + ' (household)'), reconciled: false };
    S.payments.push(p);
    audit(hh.name, 'Household', 'BANK_RECEIPT_SUBMITTED', 'payment', p.id, null, { amount: amount, reference: form.reference, bank: form.bank });
    notify('household:' + systemId, 'payment_pending', 'Bank transfer of ' + money(amount) + ' (ref ' + form.reference + ') submitted — pending verification against the bank statement.', money(amount) + ' ব্যাংক ট্রান্সফার জমা হয়েছে — ব্যাংক বিবরণীর সাথে যাচাই বাকি।');
    notify('da:' + l.daId, 'payment_pending', 'Bank transfer ' + money(amount) + ' from ' + hh.name + ' (' + systemId + ') awaiting verification — ref ' + form.reference + '.');
    S.sim.activeSystemId = systemId; markChanged('da', 'household'); persist(); return p;
  }
  function findBankMatch(p) { return (S.bankStatement || []).filter(function (b) { return !b.matchedPaymentId && b.reference === p.reference && Math.abs(diffDays(b.date, p.date)) <= 3; })[0] || null; }
  function openException(p, type, detail) { S.exceptions = S.exceptions || []; if (S.exceptions.some(function (e) { return e.paymentId === p.id && e.status !== 'Resolved'; })) return null; var e = { id: uid('EXC'), paymentId: p.id, systemId: p.systemId, daId: p.daId, type: type, detail: detail, owner: 'Accounts & Finance', status: 'Open', createdAt: nowTs() }; S.exceptions.push(e); p.exception = type; audit('system', 'Reconciliation', 'EXCEPTION_OPENED', 'payment', p.id, null, type, detail); notify('idcol', 'reconciliation', 'Reconciliation exception ' + e.id + ' (' + type + ') on ' + p.systemId + ' — ' + detail); return e; }
  function runReconciliation(actor) {
    actor = actor || 'Daily reconciliation'; var res = { checked: 0, matched: 0, exceptions: 0 };
    S.payments.filter(function (p) { return p.status === 'Pending verification'; }).forEach(function (p) { res.checked++; var b = findBankMatch(p); if (b && b.amount === p.amount) { b.matchedPaymentId = p.id; postPendingPayment(p.id, actor, b.id); res.matched++; } else if (b) { if (openException(p, 'Amount mismatch', 'Bank ' + money(b.amount) + ' vs CRM ' + money(p.amount) + ' for reference ' + p.reference)) res.exceptions++; } else if (diffDays(S.sim.today, p.date) >= 1) { if (openException(p, 'Missing bank entry', 'No bank record with reference ' + p.reference + ' in the DA Program account statement')) res.exceptions++; } });
    S.lastReconciliation = { at: nowTs(), by: actor, checked: res.checked, matched: res.matched, exceptions: res.exceptions }; audit(actor, 'IDCOL', 'RECONCILIATION_RUN', 'reconciliation', S.sim.today, null, res, 'Gateway ↔ bank ↔ cash ↔ CRM'); markChanged('idcol', 'da', 'household'); persist(); return res;
  }
  function exceptionAction(id, action, actor, note) {
    var e = byId(S.exceptions || [], id); if (!e) throw new Error('Exception not found'); var p = byId(S.payments, e.paymentId); var before = e.status;
    if (action === 'assign') { e.owner = note; e.status = e.status === 'Open' ? 'Investigating' : e.status; audit(actor, 'IDCOL', 'EXCEPTION_ASSIGNED', 'exception', e.id, before, e.status, 'Owner: ' + note); }
    else if (action === 'investigate') { e.status = 'Investigating'; e.note = note; audit(actor, 'IDCOL', 'EXCEPTION_INVESTIGATING', 'exception', e.id, before, e.status, note); }
    else if (action === 'match') { if (!note) throw new Error('Evidence reference is required to mark matched'); if (!p || p.status !== 'Pending verification') throw new Error('Only a pending transfer can be matched'); var b = findBankMatch(p); if (b) b.matchedPaymentId = p.id; postPendingPayment(p.id, actor, b ? b.id : 'evidence: ' + note); e.status = 'Resolved'; e.resolvedBy = actor; e.resolvedAt = nowTs(); e.resolution = 'Matched on evidence: ' + note; audit(actor, 'IDCOL', 'EXCEPTION_MATCHED', 'exception', e.id, before, 'Resolved', note); }
    else if (action === 'resolve') { if (!note) throw new Error('Resolution reason is required'); e.status = 'Resolved'; e.resolvedBy = actor; e.resolvedAt = nowTs(); e.resolution = note; if (p && p.exception) p.exception = null; audit(actor, 'IDCOL', 'EXCEPTION_RESOLVED', 'exception', e.id, before, 'Resolved', note); }
    markChanged('idcol', 'da'); persist(); return e;
  }
  function postPendingPayment(paymentId, actor, evidence) {
    var p = byId(S.payments, paymentId); if (!p || p.status !== 'Pending verification') throw new Error('Payment is not pending verification'); var l = loan(p.systemId); var hh = household(p.systemId);
    var remaining = p.amount + (l.credit || 0); l.credit = 0; var alloc = [];
    l.schedule.forEach(function (r) { if (remaining <= 0) return; var open = r.amount - r.paid; if (open <= 0) return; var take = Math.min(open, remaining); r.paid += take; remaining -= take; if (r.paid >= r.amount) r.paidAt = S.sim.today; alloc.push({ n: r.n, amount: take }); });
    if (remaining > 0) l.credit = remaining;
    p.status = 'Success'; p.stage = 'posted'; p.allocated = alloc; p.credit = l.credit; p.reconciled = true; p.receiptNo = 'RCPT-' + S.sim.today.replace(/-/g, '').slice(2) + '-' + pad(S.seq, 4); p.postedAt = tick(); p.verifiedBy = actor || 'Daily reconciliation';
    p.stages = (p.stages || []).concat([{ stage: 'Verified / matched', at: p.postedAt }, { stage: 'Posted to loan', at: p.postedAt }, { stage: 'Receipt available', at: p.postedAt }]); p.exception = null; p.matchEvidence = evidence || null; (S.exceptions || []).forEach(function (e) { if (e.paymentId === p.id && e.status !== 'Resolved') { e.status = 'Resolved'; e.resolvedBy = p.verifiedBy; e.resolvedAt = p.postedAt; e.resolution = 'Matched and posted'; } });
    audit(p.verifiedBy, /IDCOL|reconciliation/i.test(p.verifiedBy) ? 'IDCOL' : 'DA', 'PAYMENT_POSTED', 'payment', p.id, 'Pending verification', 'Success', 'Bank transfer matched (' + (evidence || 'statement') + ') · ref ' + p.reference);
    notify('household:' + p.systemId, 'payment_received', 'Bank transfer of ' + money(p.amount) + ' verified and posted to your loan. Receipt ' + p.receiptNo + '.', money(p.amount) + ' যাচাই হয়ে ঋণে জমা হয়েছে। রসিদ ' + p.receiptNo + '।');
    var b = balances(p.systemId); if (hh.inverterState === 'halted' && hh.haltReason === 'overdue' && b.overdue === 0) issueCommand(p.systemId, 'RESUME', 'overdue', 'system (CRM rule)', 'Overdue cleared by verified bank transfer ' + p.receiptNo); if (b.overdue === 0) l.notified = {};
    markChanged('idcol', 'da', 'household'); persist(); return p;
  }
  function requestReversal(paymentId, reason, actor, evidence, role) {
    var p = byId(S.payments, paymentId); if (!p || p.status !== 'Success') throw new Error('Only successful payments can be reversed'); if (p.reversalRequest) throw new Error('A reversal request is already pending for this transaction'); if (!reason) throw new Error('Reversal reason is required');
    S.reversalRequests = S.reversalRequests || []; var r = { id: uid('RVR'), paymentId: p.id, receiptNo: p.receiptNo, systemId: p.systemId, daId: p.daId, amount: p.amount, reason: reason, evidence: evidence || null, requestedBy: actor || 'DA agent', requestedRole: role || 'DA', requestedAt: tick(), status: 'Pending approval' };
    S.reversalRequests.push(r); p.reversalRequest = r.id;
    audit(r.requestedBy, role || 'DA', 'REVERSAL_REQUESTED', 'payment', p.id, 'Success', 'Reversal requested', reason);
    notify('idcol', 'reversal', 'Reversal request ' + r.id + ' for ' + p.receiptNo + ' (' + money(p.amount) + ') from ' + da(p.daId).name + ' — approval required.');
    markChanged('idcol', 'da'); persist(); return r;
  }
  function decideReversal(reqId, decision, actor, note) {
    var r = byId(S.reversalRequests || [], reqId); if (!r || r.status !== 'Pending approval') throw new Error('Request is not pending'); var p = byId(S.payments, r.paymentId); actor = actor || 'S. Chowdhury';
    if (actor === r.requestedBy) throw new Error('Maker-checker: the approver must be a different user from the requester (' + r.requestedBy + ')');
    r.status = decision; r.decidedBy = actor; r.decidedAt = tick(); r.note = note || null; p.reversalRequest = null;
    audit(r.decidedBy, 'IDCOL', decision === 'Approved' ? 'REVERSAL_APPROVED' : 'REVERSAL_REJECTED', 'payment', p.id, 'Reversal requested', decision, note || r.reason);
    if (decision === 'Approved') { reversePayment(p.id, r.reason + ' — request ' + r.id + ' approved', r.decidedBy); p.reversalRequestId = r.id; }
    else { p.reversalRejected = r.id; notify('da:' + p.daId, 'reversal', 'Reversal request ' + r.id + ' for ' + p.receiptNo + ' was rejected: ' + (note || '')); }
    markChanged('idcol', 'da', 'household'); persist(); return r;
  }
  function reserveSerials(daId, serials, actor) { serials.forEach(function (sn) { var u = byId(S.inventory, sn, 'serial'); if (!u || u.daId !== daId) throw new Error('Serial ' + sn + ' is not in this DA\'s inventory'); if (u.status !== 'in_stock') throw new Error('Serial ' + sn + ' is not in stock (' + u.status + ')'); u.status = 'reserved'; u.reservedAt = nowTs(); u.updatedAt = S.sim.today; }); persist(false); }
  function releaseSerials(daId, serials) { serials.forEach(function (sn) { var u = byId(S.inventory, sn, 'serial'); if (u && u.daId === daId && u.status === 'reserved') { u.status = 'in_stock'; u.reservedAt = null; u.updatedAt = S.sim.today; } }); persist(false); }
  function updateTicket(id, form, actor, role) {
    var t = byId(S.complaints, id); if (!t) throw new Error('Ticket not found'); role = role || 'DA';
    if (role === 'DA' && t.assignedTo !== 'DA') throw new Error('Ticket ' + id + ' is with IDCOL — the DA can add a note but cannot change its status');
    if (role === 'IDCOL' && form.status && ['In progress', 'Awaiting household', 'Awaiting DA', 'Awaiting IDCOL', 'Resolved'].indexOf(form.status) < 0) throw new Error('Invalid status');
    var before = t.status; t.history = t.history || []; if (form.status) t.status = form.status; if (form.status === 'Resolved') { t.resolution = form.note; t.resolvedBy = actor; t.resolvedAt = nowTs(); } if (form.status === 'Awaiting DA') t.assignedTo = 'DA'; if (form.status === 'Awaiting IDCOL') t.assignedTo = 'IDCOL'; t.history.push({ at: tick(), by: actor, action: form.status || 'Note', note: form.note || '' });
    audit(actor, role, form.status === 'Resolved' ? 'TICKET_RESOLVED' : 'TICKET_UPDATED', 'complaint', id, before, t.status, form.note);
    if (form.status === 'Awaiting DA') notify('da:' + t.daId, 'complaint', 'Ticket ' + id + ' returned to you by IDCOL: ' + (form.note || '')); notify('household:' + t.systemId, 'complaint', 'Update on ticket ' + id + ' (' + t.category + '): ' + (form.status || 'note') + (form.note ? ' — ' + form.note : ''));
    markChanged('da', 'household', 'idcol'); persist(); return t;
  }
  function assignTicket(id, to, actor, note) { var t = byId(S.complaints, id); if (!t) throw new Error('Ticket not found'); var before = t.assignedTo; t.assignedTo = to; t.history = t.history || []; if (to === 'DA' && t.status !== 'Resolved') t.status = 'Awaiting DA'; if (to === 'IDCOL' && t.status !== 'Resolved') t.status = 'Awaiting IDCOL'; t.history.push({ at: tick(), by: actor, action: 'Assigned to ' + to, note: note || '' }); audit(actor, 'IDCOL', 'TICKET_ASSIGNED', 'complaint', id, before, to, note); if (to === 'DA') notify('da:' + t.daId, 'complaint', 'Ticket ' + id + ' assigned to you by IDCOL: ' + (note || '')); markChanged('idcol', 'da'); persist(); return t; }
  function escalateTicket(id, reason, actor) { var t = byId(S.complaints, id); if (!t) throw new Error('Ticket not found'); if (!reason) throw new Error('Escalation reason is required'); var before = t.status; t.assignedTo = 'IDCOL'; t.status = 'Escalated to IDCOL'; t.history = t.history || []; t.history.push({ at: tick(), by: actor, action: 'Escalated to IDCOL', note: reason }); audit(actor, 'DA', 'TICKET_ESCALATED', 'complaint', id, before, t.status, reason); notify('idcol', 'complaint', 'Ticket ' + id + ' escalated by ' + da(t.daId).name + ': ' + reason); markChanged('idcol', 'da'); persist(); return t; }
  function reversePayment(paymentId, reason, actor) {
    var p = byId(S.payments, paymentId); if (!p || p.status !== 'Success') throw new Error('Only successful payments can be reversed'); var l = loan(p.systemId);
    p.allocated.forEach(function (a) { var r = l.schedule[a.n - 1]; r.paid -= a.amount; if (r.paid < r.amount) r.paidAt = null; }); l.credit = Math.max(0, (l.credit || 0) - (p.credit || 0));
    p.status = 'Reversed'; p.reversalReason = reason; p.reversedBy = actor || 'S. Chowdhury'; p.reversedAt = tick();
    var rev = { id: uid('PAY'), receiptNo: 'REV-' + p.receiptNo, systemId: p.systemId, daId: p.daId, date: S.sim.today, ts: p.reversedAt, amount: -p.amount, channel: p.channel, reference: 'REV/' + p.reference, status: 'Reversal', allocated: [], recordedBy: p.reversedBy, reversalOf: p.id, reconciled: true };
    S.payments.push(rev); audit(p.reversedBy, 'IDCOL', 'PAYMENT_REVERSED', 'payment', p.id, 'Success', 'Reversed', reason);
    notify('household:' + p.systemId, 'payment_reversed', 'Payment ' + p.receiptNo + ' was reversed: ' + reason);
    markChanged('idcol', 'da', 'household'); persist(); return rev;
  }
  function postVerifiedPayment(systemId, form, actor) {
    if (!form.reference) throw new Error('Transaction / reference number is required'); if (!form.reason) throw new Error('Reason for manual posting is required'); if (!form.channel) throw new Error('Channel / source is required');
    if (form.channel === 'Bank') { return submitBankReceipt(systemId, { amount: form.amount, bank: form.bank || 'Bank deposit', reference: form.reference, bankRecord: form.bankRecord }, actor + ' (IDCOL manual entry)'); }
    var p = postPayment(systemId, { amount: form.amount, channel: form.channel, reference: form.reference }, actor + ' (IDCOL manual entry)', {}); p.manual = true; p.evidence = form.evidence || null; p.postingReason = form.reason;
    audit(actor, 'IDCOL', 'MANUAL_PAYMENT_POSTED', 'payment', p.id, null, { amount: p.amount, channel: p.channel, reference: p.reference, evidence: form.evidence || null }, form.reason);
    S.sim.activeSystemId = systemId; markChanged('idcol', 'da', 'household'); persist(); return p;
  }
  function controlCapability(hh) { var c = cat(hh.inverterCat); var w = (c && c.api && c.api.write) || 'No'; return { model: c ? c.brand + ' ' + c.model : '—', write: w, kind: /^Yes/.test(w) ? 'full' : /^Partial/.test(w) ? 'partial' : 'none' }; }
  function issueCommand(systemId, command, trigger, operator, reason) {
    var hh = household(systemId); var tel = S.telemetry[systemId]; var ts = tick(); var cap = controlCapability(hh);
    var unsupported = cap.kind === 'none'; var fail = !unsupported && hh.inverterState === 'offline';
    var cmd = { id: uid('CMD'), ts: ts, systemId: systemId, command: command, trigger: trigger, operator: operator, result: unsupported ? 'UNSUPPORTED' : fail ? 'QUEUED' : 'ACK', latencyMs: unsupported ? 0 : fail ? 30000 : 1500 + Math.floor(Math.random() * 1500), brand: tel ? tel.brand : '—', model: cap.model, capability: cap.write, reason: unsupported ? 'Remote control unavailable for this OEM/model — field / gateway action required' + (reason ? ' · ' + reason : '') : fail ? 'Device unreachable — pending on reconnect' + (reason ? ' · ' + reason : '') : (cap.kind === 'partial' ? 'Partial capability — executed via supported method' + (reason ? ' · ' + reason : '') : (reason || null)) };
    S.commands.push(cmd);
    if (unsupported) { audit(operator, 'RMP', 'COMMAND_UNSUPPORTED', 'household', systemId, hh.inverterState, hh.inverterState, cmd.reason); notify('idcol', 'rmp', command + ' for ' + systemId + ' cannot be executed remotely (' + cap.model + ': control API ' + cap.write + ') — field / gateway action required.'); return cmd; }
    if (fail) { hh.queuedCommand = command; }
    if (!fail) { var before = hh.inverterState; if (command === 'HALT') { hh.inverterState = 'halted'; hh.haltReason = trigger; hh.haltedAt = ts; if (tel) tel.kwNow = 0; } else { hh.inverterState = 'running'; hh.haltReason = null; hh.haltedAt = null; if (tel) tel.kwNow = Math.round(hh.packageKwp * (tel.factor || 0.9) * 0.7 * 100) / 100; } audit(operator, trigger === 'manual' ? 'IDCOL' : 'CRM', command + '_ISSUED', 'household', systemId, before, hh.inverterState, reason || (trigger === 'overdue' ? 'Overdue threshold rule §3.1' : trigger)); }
    else audit(operator, 'RMP', command + '_QUEUED', 'household', systemId, hh.inverterState, hh.inverterState, 'Inverter offline — command pending on reconnect');
    var lb = loan(systemId), inst = lb ? money(lb.installment) : '';
    if (command === 'HALT' && !fail) { notify('household:' + systemId, 'halted', 'System halted — ' + (trigger === 'manual' ? 'by IDCOL: ' + reason : 'overdue ' + balances(systemId).overdueDays + ' days. Pay ' + money(balances(systemId).overdue) + ' to resume.'), trigger === 'manual' ? 'আইডিকল কর্তৃক সিস্টেম বন্ধ করা হয়েছে।' : 'সিস্টেম বন্ধ — বকেয়া ' + balances(systemId).overdueDays + ' দিন। চালু করতে ' + money(balances(systemId).overdue) + ' পরিশোধ করুন।'); notify('da:' + hh.daId, 'halted', systemId + ' halted (' + trigger + ').'); }
    if (command === 'RESUME' && !fail) { notify('household:' + systemId, 'resumed', 'Your system has been resumed. Thank you for your payment.', 'আপনার সিস্টেম পুনরায় চালু হয়েছে। ধন্যবাদ।'); notify('da:' + hh.daId, 'resumed', systemId + ' resumed (' + trigger + ').'); }
    return cmd;
  }
  function manualCommand(systemId, command, reason, operator) { if (!reason) throw new Error('Reason is required for manual override'); var c = issueCommand(systemId, command, 'manual', operator || 'A. Karim', reason); S.sim.activeSystemId = systemId; markChanged('idcol', 'da', 'household'); persist(); return c; }
  function submitComplaint(systemId, form) { var hh = household(systemId); var idcolCat = form.category === 'Halt / suspension dispute'; var high = /Halt|Payment not posted|offline|Wrong payment/.test(form.category); var by = form.by || (hh.name + ' (household)'); var t = { id: uid('TKT'), systemId: systemId, daId: hh.daId, category: form.category, description: form.description, paymentRef: form.paymentRef || null, status: idcolCat ? 'Awaiting IDCOL' : 'Open', priority: high ? 'High' : 'Medium', assignedTo: idcolCat ? 'IDCOL' : 'DA', source: form.source || 'Household', createdAt: nowTs(), history: [{ at: nowTs(), by: by, action: 'Submitted', note: form.description }] }; S.complaints.push(t); notify('da:' + hh.daId, 'complaint', 'New ticket ' + t.id + ' — ' + hh.name + ' (' + form.category + ')' + (idcolCat ? ' — awaiting IDCOL' : ' — assigned to you') + '.'); notify('idcol', 'complaint', 'Ticket ' + t.id + ' raised on ' + systemId + ' (' + form.category + ').'); audit(by, form.source || 'Household', 'COMPLAINT_SUBMITTED', 'complaint', t.id, null, { category: form.category, assignedTo: t.assignedTo }); markChanged('da', 'idcol', 'household'); persist(); return t; }
  function tscDecision(appId, decision, ref, actor, reason) { var a = byId(S.enlistment, appId); if (!a) return null; var before = a.status; a.status = decision; a.tscRef = ref; a.tscDate = S.sim.today; a.reason = reason || null; if (decision === 'Approved') { var id = uid('CAT'); S.catalogue.push({ id: id, category: a.category, brand: a.brand, model: a.model, keySpec: a.keySpec, certRef: a.docs.filter(function (d) { return /IEC/.test(d); }).join(' / ') || 'per application', tscRef: ref, tscDate: S.sim.today, status: 'active', prefix: (a.brand.slice(0, 3) + a.model.replace(/[^0-9]/g, '').slice(0, 3)).toUpperCase(), api: a.api || null }); } audit(actor || 'TSC', 'IDCOL', 'TSC_' + decision.toUpperCase(), 'enlistment', a.id, before, decision, reason); markChanged('idcol'); persist(); return a; }
  function setCatalogueStatus(id, status, actor, reason) { var c = cat(id); var before = c.status; c.status = status; audit(actor || 'TSC', 'IDCOL', 'CATALOGUE_STATUS', 'catalogue', id, before, status, reason); markChanged('idcol', 'da'); persist(); return c; }
  function saveSettings(patch, actor, reason) { Object.keys(patch).forEach(function (k) { var before = S.settings[k]; S.settings[k] = patch[k]; audit(actor || 'T. Ahmed', 'IDCOL', 'SETTINGS_CHANGED', 'settings', k, before, patch[k], reason || 'Configurable financial parameter'); }); markChanged('idcol'); persist(); }
  function proposeSettings(patch, actor, reason) { if (!reason) throw new Error('Reason is required'); S.settingsPending = { patch: patch, by: actor, at: nowTs(), reason: reason }; audit(actor, 'IDCOL', 'SETTINGS_PROPOSED', 'settings', Object.keys(patch).join(','), null, patch, reason); notify('idcol', 'settings', 'Settings change proposed by ' + actor + ' — confirmation by a second authorised user required.'); markChanged('idcol'); persist(); return S.settingsPending; }
  function confirmSettings(actor) { var p = S.settingsPending; if (!p) throw new Error('Nothing pending'); if (p.by === actor) throw new Error('Maker-checker: a different authorised user must confirm the change proposed by ' + p.by); saveSettings(p.patch, actor, p.reason + ' (proposed by ' + p.by + ', confirmed by ' + actor + ')'); S.settingsPending = null; persist(); }
  function discardSettings(actor, note) { var p = S.settingsPending; if (!p) return; audit(actor, 'IDCOL', 'SETTINGS_DISCARDED', 'settings', Object.keys(p.patch).join(','), p.patch, null, note); S.settingsPending = null; markChanged('idcol'); persist(); }
  function markUnitStatus(serial, status, actor, reason) { var u = byId(S.inventory, serial, 'serial'); var before = u.status; u.status = status; u.updatedAt = S.sim.today; audit(actor || 'DA agent', 'DA', 'INVENTORY_MOVEMENT', 'inventory', serial, before, status, reason); markChanged('idcol', 'da'); persist(); return u; }
  function toggleDisbursementBlock(daId, on, actor, reason) { var d = da(daId); d.disbursementBlock = !!on; audit(actor || 'T. Ahmed', 'IDCOL', 'DISBURSEMENT_BLOCK', 'da', daId, !on, !!on, reason); markChanged('idcol', 'da'); persist(); }
  function setNotificationsRead(audience) { S.notifications.forEach(function (n) { if (n.audience === audience) n.read = true; }); persist(false); }

  /* ---------- clock / daily rules ---------- */
  function advanceClock(days, opts) {
    opts = opts || {}; var changed = { idcol: false, da: false, household: false };
    for (var d = 0; d < days; d++) {
      S.sim.today = addDays(S.sim.today, 1); S.sim.clock = '08:00'; var today = S.sim.today;
      S.sim.clock = '07:30'; (function () { var res = { checked: 0, matched: 0, exceptions: 0 }; S.payments.filter(function (p) { return p.status === 'Pending verification' && p.date < today; }).forEach(function (p) { res.checked++; var b = findBankMatch(p); if (b && b.amount === p.amount) { b.matchedPaymentId = p.id; postPendingPayment(p.id, 'Daily reconciliation', b.id); res.matched++; changed.household = changed.da = true; } else if (b) { if (openException(p, 'Amount mismatch', 'Bank ' + money(b.amount) + ' vs CRM ' + money(p.amount) + ' for reference ' + p.reference)) { res.exceptions++; changed.idcol = true; } } else { if (openException(p, 'Missing bank entry', 'No bank record with reference ' + p.reference + ' in the DA Program account statement')) { res.exceptions++; changed.idcol = true; } } }); if (res.checked) S.lastReconciliation = { at: today + 'T07:30', by: 'Daily reconciliation', checked: res.checked, matched: res.matched, exceptions: res.exceptions }; })(); S.sim.clock = '08:00';
      S.households.forEach(function (hh) {
        var tel = S.telemetry[hh.systemId]; var l = loan(hh.systemId);
        if (tel) { if (hh.inverterState === 'offline') { /* no data */ } else { var gen = hh.inverterState === 'halted' ? 0 : Math.round(hh.packageKwp * 4.3 * (tel.factor || 0.9) * (0.85 + Math.random() * 0.3) * 10) / 10; var cons = Math.round(gen * 0.55 * 10) / 10; tel.daily.push({ date: today, gen: gen, cons: cons, exp: Math.max(0, Math.round((gen - cons) * 10) / 10) }); tel.cumulativeKwh += gen; tel.lastComm = today + 'T08:00'; if (tel.daily.length > 400) tel.daily.shift(); } if (hh.inverterState === 'offline' && hh.offlineSince && diffDays(today, hh.offlineSince) >= S.settings.connectivityHaltDays && !hh.pendingConnectivityHalt) { hh.pendingConnectivityHalt = true; notify('idcol', 'connectivity', hh.systemId + ' out of network ' + diffDays(today, hh.offlineSince) + ' days — connectivity halt pending.'); changed.idcol = true; } }
        if (!l) return;
        if (hh.autopay && hh.inverterState !== 'halted') { l.schedule.forEach(function (r) { if (r.dueDate === today && r.paid < r.amount) { var chs = ['bKash', 'Nagad', 'Cash', 'Bank']; postPayment(hh.systemId, { amount: r.amount - r.paid, channel: chs[Math.floor(Math.random() * chs.length)] }, null, { silent: true }); changed.da = true; } }); }
        var b = balances(hh.systemId); l.notified = l.notified || {};
        l.schedule.forEach(function (r) { if (r.dueDate === today && r.paid < r.amount && !l.notified['due' + r.n]) { l.notified['due' + r.n] = true; notify('household:' + hh.systemId, 'installment_due', 'Installment ' + r.n + ' of ' + money(r.amount) + ' is due today.', 'কিস্তি ' + r.n + ' — ' + money(r.amount) + ' আজ প্রদেয়।'); changed.household = true; } });
        if (b.overdue > 0) {
          var key = 'overdue' + b.next.n;
          if (b.overdueDays >= 1 && !l.notified[key]) { l.notified[key] = true; notify('household:' + hh.systemId, 'overdue', 'Installment overdue: ' + money(b.overdue) + '. Please pay to avoid a system halt.', 'কিস্তি বকেয়া: ' + money(b.overdue) + '। সিস্টেম বন্ধ এড়াতে পরিশোধ করুন।'); notify('da:' + hh.daId, 'overdue', hh.systemId + ' (' + hh.name + ') is overdue ' + money(b.overdue) + '.'); changed.household = changed.da = true; }
          if (b.overdueDays >= S.settings.pendingHaltWarningDay && !l.notified['pend' + b.next.n]) { l.notified['pend' + b.next.n] = true; var left = S.settings.haltOverdueDays + 1 - b.overdueDays; notify('household:' + hh.systemId, 'pending_halt', 'Pending halt: pay ' + money(b.overdue) + ' within ' + left + ' day(s) to avoid your system being halted.', 'সতর্কতা: ' + left + ' দিনের মধ্যে ' + money(b.overdue) + ' পরিশোধ না করলে সিস্টেম বন্ধ হবে।'); notify('idcol', 'pending_halt', hh.systemId + ' will be auto-halted in ' + left + ' day(s) (overdue ' + b.overdueDays + ' d).'); changed.household = changed.idcol = true; }
          if (b.overdueDays > S.settings.haltOverdueDays && hh.inverterState === 'running') { issueCommand(hh.systemId, 'HALT', 'overdue', 'system (CRM rule)', 'Overdue ' + b.overdueDays + ' days > ' + S.settings.haltOverdueDays + '-day threshold (§3.1)'); changed.idcol = changed.da = changed.household = true; }
        }
      });
    }
    var arr = Object.keys(changed).filter(function (k) { return changed[k]; }); S.sim.lastChanged = arr.length ? arr : ['idcol']; if (!opts.silent) persist(); return S.sim.today;
  }

  /* ---------- derived views ---------- */
  function flagsFor(systemId) { var tel = S.telemetry[systemId]; var hh = household(systemId); if (!tel) return []; var f = []; var last14 = tel.daily.slice(-14); var avg = last14.length ? last14.reduce(function (a, r) { return a + r.gen; }, 0) / last14.length : 0; var sy = hh.packageKwp ? avg / hh.packageKwp : 0; if (hh.inverterState === 'offline' || diffDays(S.sim.today, tel.lastComm.slice(0, 10)) >= 1) f.push('non_communicating'); if (hh.inverterState === 'running' && last14.length >= 5 && sy < 2.9) f.push('under_performing'); if (tel.faults.length >= 3) f.push('recurring_fault'); if (hh.inverterState === 'running' && last14.length >= 3 && last14.slice(-3).every(function (r) { return r.gen > 2 && r.exp === 0; })) f.push('not_exporting'); if (last14.some(function (r) { return r.gen > hh.packageKwp * 7; })) f.push('implausible'); return f; }
  function performance(systemId) { var tel = S.telemetry[systemId]; var hh = household(systemId); if (!tel) return null; var last14 = tel.daily.slice(-14); var gen14 = last14.reduce(function (a, r) { return a + r.gen; }, 0); var today = tel.daily.length && tel.daily[tel.daily.length - 1].date === S.sim.today ? tel.daily[tel.daily.length - 1] : { gen: 0, cons: 0, exp: 0 }; var month = tel.daily.filter(function (r) { return r.date.slice(0, 7) === S.sim.today.slice(0, 7); }).reduce(function (a, r) { return a + r.gen; }, 0); var exp = tel.daily.reduce(function (a, r) { return a + r.exp; }, 0); var sy = last14.length ? gen14 / last14.length / hh.packageKwp : 0; return { today: today.gen, month: Math.round(month * 10) / 10, cumulative: Math.round(tel.cumulativeKwh), exportTotal: Math.round(exp), specificYield: Math.round(sy * 100) / 100, pr: Math.min(0.95, Math.round(sy / 4.6 * 100) / 100), availability: hh.inverterState === 'offline' ? 0 : 100, kwNow: tel.kwNow, lastComm: tel.lastComm, flags: flagsFor(systemId) }; }
  function programKpis() {
    var today = S.sim.today; var kwp = S.households.reduce(function (a, h) { return a + h.packageKwp; }, 0); var genToday = 0, comm = 0; S.households.forEach(function (h) { var p = performance(h.systemId); if (p) { genToday += p.today; if (h.inverterState !== 'offline') comm++; } });
    var outstanding = 0, overdue = 0, due = 0, paidDue = 0, collectionsMonth = 0, overdueAccounts = 0; S.loans.forEach(function (l) { var b = balances(l.systemId); outstanding += b.remaining; overdue += b.overdue; due += b.due; paidDue += Math.min(b.paid, b.due); if (b.overdue > 0) overdueAccounts++; });
    S.payments.forEach(function (p) { if (p.status === 'Success' && p.date.slice(0, 7) === today.slice(0, 7)) collectionsMonth += p.amount; });
    return { activeDAs: S.das.filter(function (d) { return d.status === 'Active'; }).length, systems: S.households.length, kwp: kwp, genToday: Math.round(genToday * 10) / 10, communicating: comm, collectionsMonth: collectionsMonth, collectionEfficiency: due ? Math.round(paidDue / due * 1000) / 10 : 100, outstanding: outstanding, overdue: overdue, overduePct: outstanding ? Math.round(overdue / outstanding * 1000) / 10 : 0, overdueAccounts: overdueAccounts, halted: S.households.filter(function (h) { return h.inverterState === 'halted'; }).length, offline: S.households.filter(function (h) { return h.inverterState === 'offline'; }).length, claimsAwaiting: S.claims.filter(function (c) { return c.status === 'Submitted' || c.status === 'Under review'; }).length, claimsEligible: S.claims.filter(function (c) { return c.status === 'Eligible'; }).length, inspectionsPending: S.inspections.filter(function (i) { return i.result === 'Pending'; }).length, batchesPending: S.batches.filter(function (b) { return b.status === 'Pending'; }).length, flags: S.households.reduce(function (a, h) { return a + (flagsFor(h.systemId).length ? 1 : 0); }, 0), enlistPending: S.enlistment.filter(function (e) { return e.status === 'Pending'; }).length, refinanced: S.claims.filter(function (c) { return c.status === 'Paid'; }).reduce(function (a, c) { return a + c.amount; }, 0), financed: S.loans.reduce(function (a, l) { return a + l.daFinanced; }, 0) };
  }
  function daKpis(daId) {
    var hhs = S.households.filter(function (h) { return h.daId === daId; }); var loans = S.loans.filter(function (l) { return l.daId === daId; }); var today = S.sim.today;
    var outstanding = 0, overdue = 0, due = 0, paidDue = 0, par30 = 0, par90 = 0, overdueAccounts = 0, collectionsMonth = 0, financed = 0;
    loans.forEach(function (l) { var b = balances(l.systemId); financed += l.daFinanced; outstanding += b.remaining; overdue += b.overdue; due += b.due; paidDue += Math.min(b.paid, b.due); if (b.overdue > 0) overdueAccounts++; if (b.overdueDays > 30) par30 += b.remaining; if (b.overdueDays > 90) par90 += b.remaining; });
    S.payments.forEach(function (p) { if (p.daId === daId && p.status === 'Success' && p.date.slice(0, 7) === today.slice(0, 7)) collectionsMonth += p.amount; });
    var inv = S.inventory.filter(function (u) { return u.daId === daId; }); var claims = S.claims.filter(function (c) { return c.daId === daId; }); var insp = S.inspections.filter(function (i) { return i.daId === daId && i.result !== 'Pending'; });
    var ref = S.daLedgers.filter(function (l) { return l.daId === daId && l.type === 'Refinance'; })[0]; var adv = S.daLedgers.filter(function (l) { return l.daId === daId && l.type === 'Advance'; })[0];
    var genToday = 0, comm = 0; hhs.forEach(function (h) { var p = performance(h.systemId); if (p) { genToday += p.today; if (h.inverterState !== 'offline') comm++; } });
    var d = da(daId); var cover = outstanding ? Math.round(d.security.bgAmount / (ref ? Math.max(ref.outstanding, 1) : 1) * 1000) / 10 : null;
    return { systems: hhs.length, kwp: hhs.reduce(function (a, h) { return a + h.packageKwp; }, 0), inStock: inv.filter(function (u) { return u.status === 'in_stock'; }).length, installed: inv.filter(function (u) { return u.status === 'installed'; }).length, faulty: inv.filter(function (u) { return u.status === 'faulty' || u.status === 'returned'; }).length, financed: financed, outstanding: outstanding, overdue: overdue, overdueAccounts: overdueAccounts, collectionsMonth: collectionsMonth, collectionEfficiency: due ? Math.round(paidDue / due * 1000) / 10 : 100, par30: outstanding ? Math.round(par30 / outstanding * 1000) / 10 : 0, par90: outstanding ? Math.round(par90 / outstanding * 1000) / 10 : 0, claimsPaid: claims.filter(function (c) { return c.status === 'Paid'; }).length, claimsPaidAmount: claims.filter(function (c) { return c.status === 'Paid'; }).reduce(function (a, c) { return a + c.amount; }, 0), claimsPending: claims.filter(function (c) { return c.status !== 'Paid'; }).length, claimsOnHold: claims.filter(function (c) { return c.status === 'On hold'; }).length, inspectionFailureRate: insp.length ? Math.round(insp.filter(function (i) { return i.result !== 'Pass'; }).length / insp.length * 1000) / 10 : 0, telemetryAvailability: hhs.length ? Math.round(comm / hhs.length * 1000) / 10 : 100, genToday: Math.round(genToday * 10) / 10, halted: hhs.filter(function (h) { return h.inverterState === 'halted'; }).length, refinance: ref, advance: adv, securityCover: ref && ref.outstanding ? Math.round(d.security.bgAmount / ref.outstanding * 1000) / 10 : null, headroom: ref ? ref.headroom : d.facility.limit };
  }
  function ewsRows(daId) {
    var rows = []; S.das.forEach(function (d) { if (daId && d.id !== daId) return; var k = daKpis(d.id); var t = S.settings.ews; function add(metric, value, threshold, dir, sev, owner) { var breach = dir === 'min' ? value < threshold : value > threshold; rows.push({ daId: d.id, da: d.name, metric: metric, value: value, threshold: threshold, dir: dir, severity: breach ? sev : 'OK', owner: owner, action: breach ? 'Review with DA · escalate if unresolved 30 d' : '—', escalation: breach && sev === 'High' ? 'Management → Board' : breach ? 'Management' : '—' }); }
      if (k.systems === 0) return;
      add('Collection efficiency (%)', k.collectionEfficiency, t.collectionEfficiency, 'min', 'High', 'Credit Admin'); add('PaR30 (%)', k.par30, t.par30, 'max', 'High', 'Credit Admin'); add('PaR90 (%)', k.par90, t.par90, 'max', 'High', 'Credit Admin'); add('Telemetry availability (%)', k.telemetryAvailability, t.telemetryAvailability, 'min', 'Medium', 'Technical Unit'); add('Inspection failure rate (%)', k.inspectionFailureRate, t.inspectionFailureRate, 'max', 'Medium', 'Inspection Unit');
      var bgDays = diffDays(d.security.bgExpiry, S.sim.today); rows.push({ daId: d.id, da: d.name, metric: 'Bank Guarantee expiry (days)', value: bgDays, threshold: S.settings.bgNoticeDays, dir: 'min', severity: bgDays < S.settings.bgNoticeDays ? 'High' : 'OK', owner: 'Credit Admin', action: bgDays < S.settings.bgNoticeDays ? 'Renewal notice issued · disbursement block if not renewed' : '—', escalation: bgDays < S.settings.bgNoticeDays ? 'Management' : '—' });
      var cover = k.refinance && k.refinance.outstanding ? Math.round(d.security.bgAmount / k.refinance.outstanding * 1000) / 10 : 999; rows.push({ daId: d.id, da: d.name, metric: 'Security cover (BG / outstanding %)', value: cover, threshold: S.settings.securityCoverPct, dir: 'min', severity: cover < S.settings.securityCoverPct ? 'High' : 'OK', owner: 'Credit Admin', action: cover < S.settings.securityCoverPct ? 'Top-up guarantee · optional disbursement block' : '—', escalation: cover < S.settings.securityCoverPct ? 'Management → Board' : '—' });
      var dsraReq = k.refinance && k.refinance.installment ? k.refinance.installment * 3 : 0; rows.push({ daId: d.id, da: d.name, metric: 'DSRA balance vs 1 quarter', value: d.security.dsraBalance, threshold: dsraReq, dir: 'min', severity: d.security.dsraBalance < dsraReq ? 'Medium' : 'OK', owner: 'Accounts & Finance', action: d.security.dsraBalance < dsraReq ? 'DSRA top-up request' : '—', escalation: d.security.dsraBalance < dsraReq ? 'Management' : '—', money: true });
    }); return rows;
  }
  function tenorMismatch(daId) { var d = da(daId); var ref = S.daLedgers.filter(function (l) { return l.daId === daId && l.type === 'Refinance'; })[0]; if (!ref || !ref.schedule.length) return { after60: 0, monthly: 0 }; var after = ref.schedule.slice(60).reduce(function (a, r) { return a + r.principal; }, 0); return { after60: after, monthly: ref.installment, months: 24 }; }
  function haltQueues() { var q = { autoDue: [], halted: [], connectivity: [], resumeDue: [] }; S.households.forEach(function (h) { var b = balances(h.systemId) || {}; if (h.inverterState === 'halted') { q.halted.push({ hh: h, b: b }); if (h.haltReason === 'overdue' && b.overdue === 0) q.resumeDue.push({ hh: h, b: b }); } else if (h.inverterState === 'running' && b.overdue > 0 && b.overdueDays >= S.settings.pendingHaltWarningDay) q.autoDue.push({ hh: h, b: b, daysLeft: Math.max(0, S.settings.haltOverdueDays + 1 - b.overdueDays) }); if (h.inverterState === 'offline') q.connectivity.push({ hh: h, b: b, days: h.offlineSince ? diffDays(S.sim.today, h.offlineSince) : 0 }); }); return q; }
  function reconciliation(daId, days) { var rows = []; var from = addDays(S.sim.today, -(days || 14)); S.payments.forEach(function (p) { if (daId && p.daId !== daId) return; if (p.date < from) return; var gw = (p.channel === 'bKash' || p.channel === 'Nagad' || p.channel === 'Card') && p.status !== 'Failed' ? p.amount : null; var bnk = findBankMatch(p) || (S.bankStatement || []).filter(function (b) { return b.matchedPaymentId === p.id; })[0]; var bank = p.channel === 'Bank' ? (bnk ? bnk.amount : (p.status === 'Success' && p.reconciled ? p.amount : null)) : (gw != null && p.status !== 'Reversal' ? p.amount : null); var crm = p.status === 'Pending verification' || p.status === 'Failed' ? null : p.amount; var ex = (S.exceptions || []).filter(function (e) { return e.paymentId === p.id; }).sort(function (a, b) { return a.status === 'Resolved' ? 1 : -1; })[0]; var status = p.status === 'Reversed' ? 'Reversed' : p.status === 'Reversal' ? 'Reversal entry' : p.status === 'Refunded' ? 'Refunded' : p.status === 'Failed' ? 'Failed' : p.status === 'Pending verification' ? (ex && ex.status !== 'Resolved' ? 'Unmatched' : 'Pending') : p.channel === 'Cash' && !p.reconciled ? 'Unmatched' : 'Matched'; rows.push({ id: p.id, date: p.date, receipt: p.receiptNo, reference: p.reference, systemId: p.systemId, daId: p.daId, channel: p.channel, gateway: gw, bank: bank, crm: crm, status: status, exceptionType: ex ? ex.type : (status === 'Unmatched' ? 'Unmatched' : null), exception: ex || null, owner: ex ? ex.owner : null, resolution: ex ? ex.status : null, paymentStatus: p.status }); }); return rows.sort(function (a, b) { return a.date < b.date ? 1 : -1; }); }
  function notificationsFor(audience) { return S.notifications.filter(function (n) { return n.audience === audience; }).sort(function (a, b) { return a.createdAt < b.createdAt ? 1 : -1; }); }
  function brandMatrix() { return S.catalogue.filter(function (c) { return c.category === 'inverter'; }).map(function (c) { return { brand: c.brand, model: c.model, status: c.status, api: c.api || { read: '—', write: '—', terms: '—', fallback: '—' }, systems: S.households.filter(function (h) { return h.inverterCat === c.id; }).length }; }); }

  /* ---------- scenario ---------- */
  var SCENARIO = [
    { n: 1, title: 'IDCOL onboards a new Demand Aggregator', who: 'IDCOL Staff', modules: ['MIS'], refs: '§3.3.2 DA master · facility · security register', desc: 'IDCOL creates "Sundarban Solar Foundation" (NGO, Khulna & Satkhira) with a ৳50 Cr refinance limit, ৳5 Cr advance facility and a ৳10 Cr Bank Guarantee. Two DA ledgers (Advance, Refinance) are opened. The DA appears in the DA panel and in every program-wide view.', changed: ['idcol', 'da'],
      run: function () { if (!da('DA-004')) onboardDA({ id: 'DA-004', name: 'Sundarban Solar Foundation', legalForm: 'NGO', registrationNo: 'NGOAB-0417', districts: ['Khulna', 'Satkhira'], keyPersons: ['Dr. Selina Haque — Executive Director', 'Rafiq Mollah — Head of Finance'], solarTrackRecord: '3,200 SHS 2012–2019; 12 rooftop pilots 2025', limit: 500000000, advanceLimit: 50000000, bg: 100000000, bgBank: 'Janata Bank', bgExpiry: '2027-09-30', dsra: 2500000, pg: ['Dr. Selina Haque (ED)'], cg: ['Sundarban Trust'] }, 'A. Karim'); else { S.sim.activeDaId = 'DA-004'; markChanged('idcol', 'da'); persist(); } } },
    { n: 2, title: 'PSI batches verified and allotted to the DA', who: 'IDCOL Technical Unit', modules: ['MIS'], refs: '§3.3.1 batch/lot verification · serialised inventory · §5.1 CSV import', desc: 'The new DA declares two lots with PSI reports — Growatt MIN 5000TL-X × 20 and Jinko Tiger Neo 550 W × 160 (serial lists imported by CSV) plus BYD HVS 5.1 × 10. IDCOL verifies the PSI reports and allots them: 190 serials appear as "in stock" in the DA\'s walled inventory and nowhere else.', changed: ['idcol', 'da'],
      run: function () { if (!da('DA-004')) SCENARIO[0].run(); if (!byId(S.batches, 'B-1041')) { declareBatch('DA-004', { id: 'B-1041', catalogueId: 'CAT-I01', quantity: 20, supplier: 'Growatt New Energy', psiRef: 'PSI-1041-2026' }, 'DA-004 agent'); declareBatch('DA-004', { id: 'B-1042', catalogueId: 'CAT-P01', quantity: 160, supplier: 'Jinko Solar (BD)', psiRef: 'PSI-1042-2026' }, 'DA-004 agent'); declareBatch('DA-004', { id: 'B-1043', catalogueId: 'CAT-B01', quantity: 10, supplier: 'BYD Battery (BD)', psiRef: 'PSI-1043-2026' }, 'DA-004 agent'); } ['B-1041', 'B-1042', 'B-1043'].forEach(function (id) { verifyBatch(id, 'M. Hasan'); }); S.sim.activeDaId = 'DA-004'; markChanged('idcol', 'da'); persist(); } },
    { n: 3, title: 'DA onboards a household — serials drawn from its own stock', who: 'DA Agent', modules: ['MIS', 'CRM'], refs: '§3.3.1 household onboarding (no free text) · DA field report · §3.5.2 loan ledger & schedule', desc: 'Rahima Begum, Dumuria (Khulna), 4 kWp package at ৳3,40,000 (ceiling ৳3,60,000). The DA picks 8 panel serials, 1 inverter and 1 battery from drop-downs limited to its in-stock units, uploads its field-inspection report with installer sign-off and confirms. System ID DRS-KHU-0213 is created, serials become "installed", a 60-month schedule is generated and IDCOL receives an inspection request.', changed: ['idcol', 'da', 'household'],
      run: function () { if (!household('DRS-KHU-0213')) { if (!byId(S.batches, 'B-1041') || byId(S.batches, 'B-1041').status !== 'Verified') SCENARIO[1].run(); var panels = stockFor('DA-004', 'CAT-P01').slice(0, 8).map(function (u) { return u.serial; }); var inv = stockFor('DA-004', 'CAT-I01')[0].serial; var bat = stockFor('DA-004', 'CAT-B01')[0].serial; onboardHousehold('DA-004', { systemId: 'DRS-KHU-0213', name: 'Rahima Begum', nid: '1989123400213', phone: '01811-560213', district: 'Khulna', upazila: 'Dumuria', address: 'Village Sahos, Dumuria', gps: { lat: 22.8081, lng: 89.4231 }, kwp: 4, contractPrice: 340000, panelSerials: panels, inverterSerial: inv, batterySerial: bat, refinance: 240000, daReport: { installer: 'Md. Abdur Rouf (Team KHU-2)', photos: 6 } }, 'Sundarban field agent'); } else { S.sim.activeSystemId = 'DRS-KHU-0213'; S.sim.activeDaId = 'DA-004'; markChanged('idcol', 'da', 'household'); persist(); } } },
    { n: 4, title: 'IDCOL inspection — serial cross-check passes', who: 'IDCOL Inspector', modules: ['Inspection'], refs: '§3.4.1 serial cross-verification · §3.4.2 eligibility determination & payment trigger', desc: 'The inspector scans the serials found on the roof; the system cross-checks them against the DA\'s onboarding record — all match. Checklist complete, geotagged photos attached, inspector signs off: result Pass. The refinance claim flips from "Not eligible" to "Eligible" (৳2,40,000 shown for verification). Both the DA report and the IDCOL inspection are retained, tagged by source.', changed: ['idcol', 'da', 'household'],
      run: function () { if (!household('DRS-KHU-0213')) SCENARIO[2].run(); var insp = S.inspections.filter(function (i) { return i.systemId === 'DRS-KHU-0213'; })[0]; var hh = household('DRS-KHU-0213'); if (insp.result === 'Pending') recordInspection(insp.id, { scanned: { panels: hh.panelSerials.slice(), inverter: hh.inverterSerial, battery: hh.batterySerial }, gpsDistanceM: 14, photos: 6 }, 'R. Sultana'); else { S.sim.activeSystemId = 'DRS-KHU-0213'; markChanged('idcol', 'da'); persist(); } } },
    { n: 5, title: 'DA submits the refinance claim; IDCOL reviews, approves (maker-checker) and disburses', who: 'DA Agent → IDCOL Accounts & Finance', modules: ['Inspection', 'MIS'], refs: '§3.4.2 inspection-to-claim linkage · §5.2 maker-checker · duplicate financing control', desc: 'The DA prepares the claim for the inspected system (eligible amount ৳2,40,000, supporting documents attached) and submits it. A. Karim reviews it; S. Chowdhury approves — approval by the same user is refused; the DA cannot approve its own claim. On disbursement the DA\'s refinance ledger shows ৳2,40,000 drawn and headroom reduced; the DA sees the claim as Paid.', changed: ['idcol', 'da'],
      run: function () { if (!household('DRS-KHU-0213')) SCENARIO[3].run(); var c = byId(S.claims, 'DRS-KHU-0213', 'systemId'); if (c.status === 'Not eligible') SCENARIO[3].run(); c = byId(S.claims, 'DRS-KHU-0213', 'systemId'); if (c.status === 'Eligible') claimAction(c.id, 'draft', 'Sundarban Solar Foundation agent', null, 'DA'); if (c.status === 'Draft') claimAction(c.id, 'submit', 'Sundarban Solar Foundation agent', null, 'DA'); if (c.status === 'Submitted') claimAction(c.id, 'prepare', 'A. Karim'); if (c.status === 'Under review') claimAction(c.id, 'approve', 'S. Chowdhury'); if (c.status === 'Approved') claimAction(c.id, 'disburse', 'T. Ahmed'); S.sim.activeDaId = 'DA-004'; markChanged('idcol', 'da'); persist(); } },
    { n: 6, title: 'Household pays the first installment via bKash', who: 'Household', modules: ['CRM'], refs: '§3.6 digital gateway · §3.5.2 payment allocation · digital receipt', desc: 'The simulated clock moves to the first due date (11 Oct 2026). Rahima pays ৳5,260 through the bKash checkout; the gateway posts it to the CRM in real time, the amount is allocated to installment 1, a receipt number is issued and all three profiles see the collection.', changed: ['idcol', 'da', 'household'],
      run: function () { if (!household('DRS-KHU-0213')) SCENARIO[4].run(); var l = loan('DRS-KHU-0213'); if (S.sim.today < l.commencement) advanceClock(diffDays(l.commencement, S.sim.today), { silent: true }); if (l.schedule[0].paid < l.schedule[0].amount) recordPayment('DRS-KHU-0213', { amount: l.schedule[0].amount, channel: 'bKash' }); else { S.sim.activeSystemId = 'DRS-KHU-0213'; markChanged('household'); persist(); } } },
    { n: 7, title: 'One month later — installment 2 falls due, not paid', who: 'System (clock +31 days)', modules: ['CRM'], refs: '§5.1 SMS + in-app notification: installment due', desc: 'The clock advances to 11 Nov 2026. Installment 2 (৳5,260) is due today; the household receives an "installment due" notification (SMS + in-app) and the DA sees the account as due. No payment is made.', changed: ['da', 'household'],
      run: function () { if (!household('DRS-KHU-0213')) SCENARIO[5].run(); var l = loan('DRS-KHU-0213'); var target = l.schedule[1].dueDate; if (S.sim.today < target) advanceClock(diffDays(target, S.sim.today)); else { markChanged('household'); persist(); } S.sim.activeSystemId = 'DRS-KHU-0213'; persist(); } },
    { n: 8, title: 'Overdue 8 days — pending-halt warning', who: 'System (clock +8 days)', modules: ['CRM'], refs: '§3.5.2 overdue calculation · §5.1 pending-halt notification', desc: 'Eight days past the due date the CRM computes ৳5,260 overdue / 8 days. The household is warned that the system will be halted if payment is not received within 3 days; IDCOL\'s Halt/Resume Control lists the account under "Auto-halt due".', changed: ['idcol', 'da', 'household'],
      run: function () { if (!household('DRS-KHU-0213')) SCENARIO[6].run(); var l = loan('DRS-KHU-0213'); var target = addDays(l.schedule[1].dueDate, 8); if (S.sim.today < target) advanceClock(diffDays(target, S.sim.today)); else { markChanged('household'); persist(); } S.sim.activeSystemId = 'DRS-KHU-0213'; persist(); } },
    { n: 9, title: 'Overdue 11 days — CRM decides HALT, RMP executes it', who: 'System (clock +3 days)', modules: ['CRM', 'RMP'], refs: '§3.1 halt within 10 days · §3.5.1 execution via RMP · §3.2.1 command logging', desc: 'On day 11 the overdue exceeds the 10-day threshold. The CRM decides HALT; the RMP integration layer sends the command through the Growatt adapter and logs timestamp, system ID, command, trigger and result (ACK). The inverter stops; the household home shows a red "System halted — pay ৳5,260 to resume" banner; DA and IDCOL views show Halted.', changed: ['idcol', 'da', 'household'],
      run: function () { if (!household('DRS-KHU-0213')) SCENARIO[7].run(); var l = loan('DRS-KHU-0213'); var target = addDays(l.schedule[1].dueDate, 11); if (S.sim.today < target) advanceClock(diffDays(target, S.sim.today)); else { markChanged('household'); persist(); } S.sim.activeSystemId = 'DRS-KHU-0213'; persist(); } },
    { n: 10, title: 'Household pays via Nagad — RESUME issued automatically', who: 'Household', modules: ['CRM', 'RMP'], refs: '§3.1 resume · §3.5.2 payment history & receipt', desc: 'Rahima pays the overdue ৳5,260 through Nagad. The CRM clears the overdue, decides RESUME, RMP executes and logs it (ACK); the inverter runs again, the household banner turns green and the DA collection ledger and IDCOL command log update.', changed: ['idcol', 'da', 'household'],
      run: function () { if (!household('DRS-KHU-0213')) SCENARIO[8].run(); var b = balances('DRS-KHU-0213'); if (b.overdue > 0) recordPayment('DRS-KHU-0213', { amount: b.overdue, channel: 'Nagad' }); else if (household('DRS-KHU-0213').inverterState === 'halted') manualCommand('DRS-KHU-0213', 'RESUME', 'Overdue cleared', 'system (CRM rule)'); else { markChanged('household'); persist(); } } },
    { n: 11, title: 'IDCOL manual override — halt & resume with reason, fully logged', who: 'IDCOL Staff', modules: ['CRM', 'RMP'], refs: '§3.5.1 manual override · §4 audit trail', desc: 'An authorised IDCOL agent halts DRS-GAZ-0032 (recurring isolation fault F-23, safety) with a reason, then resumes it after the DA\'s technician clears the fault. Both commands appear in the RMP command log with trigger "manual" and the operator name, and in the audit trail.', changed: ['idcol', 'da', 'household'],
      run: function () { var hh = household('DRS-GAZ-0032'); if (hh.inverterState === 'running') { manualCommand('DRS-GAZ-0032', 'HALT', 'Safety: recurring isolation fault F-23 — DA technician dispatched', 'A. Karim'); } if (hh.inverterState === 'halted') { S.telemetry['DRS-GAZ-0032'].faults = []; manualCommand('DRS-GAZ-0032', 'RESUME', 'Fault cleared and verified by DA technician', 'A. Karim'); } S.sim.activeDaId = 'DA-001'; S.sim.activeSystemId = 'DRS-GAZ-0032'; markChanged('idcol', 'da', 'household'); persist(); } }
  ];
  function scenarioStep() { return S.sim.step; }
  function scenarioNext() { if (S.sim.step >= SCENARIO.length) return null; var st = SCENARIO[S.sim.step]; st.run(); S.sim.step++; S.sim.scenarioLog.push({ n: st.n, at: nowTs(), title: st.title }); S.sim.lastChanged = st.changed.slice(); persist(); return st; }
  function scenarioGoto(n) { var s = seed(); S = s; for (var i = 0; i < n; i++) { SCENARIO[i].run(); S.sim.step = i + 1; S.sim.scenarioLog.push({ n: i + 1, at: nowTs(), title: SCENARIO[i].title }); } if (n === 0) S.sim.lastChanged = []; else S.sim.lastChanged = SCENARIO[n - 1].changed.slice(); persist(); return S; }
  function scenarioPrev() { return scenarioGoto(Math.max(0, S.sim.step - 1)); }

  var api = { KEY: KEY, load: load, state: function () { return load(); }, persist: persist, reset: reset, onChange: onChange, seed: seed,
    fmt: { money: money, num: num, pct: pct, date: fmtDate, dt: fmtDT, addDays: addDays, addMonths: addMonths, diffDays: diffDays, pad: pad, groupBD: groupBD },
    get: { da: da, household: household, loan: loan, cat: cat, pkg: pkgOf, serialsFor: serialsFor, balances: balances, rowStatus: rowStatus, stockFor: stockFor, daModels: daModels, flagsFor: flagsFor, performance: performance, programKpis: programKpis, daKpis: daKpis, ewsRows: ewsRows, tenorMismatch: tenorMismatch, haltQueues: haltQueues, reconciliation: reconciliation, notificationsFor: notificationsFor, brandMatrix: brandMatrix, controlCapability: controlCapability, findBankMatch: findBankMatch, checklist: function () { return CHECKLIST.slice(); } },
    act: { onboardDA: onboardDA, declareBatch: declareBatch, verifyBatch: verifyBatch, onboardHousehold: onboardHousehold, recordInspection: recordInspection, claimAction: claimAction, recordPayment: recordPayment, submitBankReceipt: submitBankReceipt, postPendingPayment: postPendingPayment, postVerifiedPayment: postVerifiedPayment, runReconciliation: runReconciliation, exceptionAction: exceptionAction, requestReversal: requestReversal, decideReversal: decideReversal, reserveSerials: reserveSerials, releaseSerials: releaseSerials, updateTicket: updateTicket, assignTicket: assignTicket, escalateTicket: escalateTicket, proposeSettings: proposeSettings, confirmSettings: confirmSettings, discardSettings: discardSettings, manualCommand: manualCommand, submitComplaint: submitComplaint, tscDecision: tscDecision, setCatalogueStatus: setCatalogueStatus, saveSettings: saveSettings, markUnitStatus: markUnitStatus, toggleDisbursementBlock: toggleDisbursementBlock, advanceClock: advanceClock, setNotificationsRead: setNotificationsRead, setActiveDa: function (id) { S.sim.activeDaId = id; persist(); }, setActiveSystem: function (id) { S.sim.activeSystemId = id; persist(); }, setLang: function (l) { S.sim.lang = l; persist(false); } },
    scenario: { steps: SCENARIO, step: scenarioStep, next: scenarioNext, prev: scenarioPrev, goto: scenarioGoto } };
  if (typeof module !== 'undefined' && module.exports) module.exports = api; else global.DRS = api;
})(typeof window !== 'undefined' ? window : this);
